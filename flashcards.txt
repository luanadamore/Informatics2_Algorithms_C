#separator:tab
#html:true
#notetype:Basic
#tags column:3
"<b>Bubble Sort</b> — Explain how it works (steps, ~4 bullets), code it in C, and state time &amp; space complexity (best / avg / worst, space)."	"<b>Principle (steps):</b>
<ol>
<li>Walk through the array from left to right.</li>
<li>For every adjacent pair, if the left element is greater than the right, swap them.</li>
<li>After one full pass, the largest unsorted element has 'bubbled up' to the end → it is now in its final position.</li>
<li>Repeat for the remaining unsorted prefix. After <i>i</i> passes the last <i>i</i> elements are sorted.</li>
</ol>
<b>C code:</b>
<pre><code>void bubble_sort(int a[], int n) {
    // i counts how many passes we have done
    // after pass i the last i elements are already in place
    for (int i = 0; i &lt; n - 1; i++) {
        // j stops at n-1-i because the last i elements are already sorted
        for (int j = 0; j &lt; n - 1 - i; j++) {
            if (a[j] &gt; a[j+1]) {
                int tmp = a[j];
                a[j]    = a[j+1];
                a[j+1]  = tmp;
            }
        }
    }
}</code></pre>
<b>Complexity:</b>
<ul>
<li>Time — Best / Average / Worst: <b>O(n²)</b>.</li>
<li>Space: <b>O(1)</b>, in-place.</li>
</ul>"	Info2::Sorting
"<b>Selection Sort</b> — Explain how it works (steps, ~4 bullets), code it in C, and state time &amp; space complexity (best / avg / worst, space)."	"<b>Principle (steps):</b>
<ol>
<li>Split the array conceptually into a sorted prefix (initially empty) and an unsorted suffix.</li>
<li>Scan the unsorted suffix to find the minimum element.</li>
<li>Swap that minimum with the first element of the unsorted suffix → the sorted prefix grows by one.</li>
<li>Repeat until the unsorted suffix is empty. Exactly <i>n − 1</i> swaps in total.</li>
</ol>
<b>C code:</b>
<pre><code>void selection_sort(int a[], int n) {
    // i = boundary; the sorted prefix is a[0..i-1]
    for (int i = 0; i &lt; n - 1; i++) {
        // min_idx = index of the smallest element found so far in the unsorted part
        int min_idx = i;
        // scan the unsorted suffix [i+1, n)
        for (int j = i + 1; j &lt; n; j++) {
            if (a[j] &lt; a[min_idx]) {
                min_idx = j;
            }
        }
        // put the minimum at position i (skip swap if it's already there)
        if (min_idx != i) {
            int tmp     = a[i];
            a[i]        = a[min_idx];
            a[min_idx]  = tmp;
        }
    }
}</code></pre>
<b>Complexity:</b>
<ul>
<li>Time — Best / Average / Worst: <b>O(n²)</b> (always scans the full suffix, no early exit).</li>
<li>Space: <b>O(1)</b>, in-place.</li>
<li>Nice property: only <b>O(n)</b> swaps total → good when writes are expensive.</li>
</ul>"	Info2::Sorting
"<b>Insertion Sort</b> — Explain how it works (steps, ~5 bullets), code it in C, and state time &amp; space complexity (best / avg / worst, space)."	"<b>Principle (steps):</b>
<ol>
<li>Treat the first element as a sorted sub-array of length 1.</li>
<li>Take the next element (the <i>key</i>) from the unsorted part.</li>
<li>Shift every element of the sorted part that is greater than the key one position to the right.</li>
<li>Insert the key into the gap so the sorted part is sorted again.</li>
<li>Repeat for every remaining element. Sorted region grows by 1 each iteration.</li>
</ol>
<b>C code:</b>
<pre><code>void insertion_sort(int a[], int n) {
    // i = first index of the unsorted part; sorted part is a[0..i-1]
    for (int i = 1; i &lt; n; i++) {
        // key = element we are inserting into the sorted prefix
        int key = a[i];
        // j walks left through the sorted prefix
        int j   = i - 1;
        // shift every element greater than key one position to the right
        while (j &gt;= 0 &amp;&amp; a[j] &gt; key) {
            a[j+1] = a[j];
            j--;
        }
        // j+1 is the correct slot for key (the gap left by the shifting)
        a[j+1] = key;
    }
}</code></pre>
<b>Complexity:</b>
<ul>
<li>Time — Best: <b>O(n)</b> (already sorted, inner while exits immediately). Average / Worst: <b>O(n²)</b>.</li>
<li>Space: <b>O(1)</b>, in-place.</li>
<li>Excellent for <b>small</b> or <b>nearly-sorted</b> arrays.</li>
</ul>"	Info2::Sorting
"<b>Quick Sort</b> — Explain how it works (steps, ~4 bullets), code it in C (Hoare partition), and state time &amp; space complexity (best / avg / worst, space)."	"<b>Principle (steps):</b>
<ol>
<li>Base case: a sub-array of length ≤ 1 is sorted → return.</li>
<li>Pick a <i>pivot</i> (we use <code>a[lo]</code> here; see the pivot-choice card for better options).</li>
<li><b>Partition (Hoare):</b> two pointers walk towards each other from both ends, swapping out-of-place elements, until they cross. Result: every element left of the split is ≤ pivot, every element right of the split is ≥ pivot.</li>
<li>Recursively quicksort both halves. With Hoare you recurse on <code>[lo, p]</code> and <code>[p+1, hi]</code> (note: <code>p</code>, not <code>p−1</code>, because the pivot is not guaranteed to land at index <code>p</code>).</li>
</ol>
<b>C code (Hoare partition):</b>
<pre><code>static void swap(int *x, int *y) {
    int tmp = *x; *x = *y; *y = tmp;
}

// Hoare partition: returns an index p such that
//   every element in a[lo..p]   is &lt;= pivot,
//   every element in a[p+1..hi] is &gt;= pivot.
// (The pivot itself can end up anywhere on the left side.)
int hoare_partition(int a[], int lo, int hi) {
    // pivot value (any choice works; we take the first element)
    int pivot = a[lo];
    // i = left pointer, starts just OUTSIDE the range; will move right
    int i = lo - 1;
    // j = right pointer, starts just OUTSIDE the range; will move left
    int j = hi + 1;
    while (1) {
        // advance i until we find an element NOT smaller than pivot
        do { i++; } while (a[i] &lt; pivot);
        // advance j until we find an element NOT greater than pivot
        do { j--; } while (a[j] &gt; pivot);
        // if the pointers crossed, the partition is complete
        if (i &gt;= j) return j;
        // otherwise a[i] and a[j] are on the wrong sides → swap them
        swap(&amp;a[i], &amp;a[j]);
    }
}

void quick_sort(int a[], int lo, int hi) {
    if (lo &lt; hi) {
        int p = hoare_partition(a, lo, hi);
        // with Hoare we use p (NOT p-1) for the left recursive call
        quick_sort(a, lo, p);
        quick_sort(a, p + 1, hi);
    }
}</code></pre>
<b>Complexity:</b>
<ul>
<li>Time — Best / Average: <b>O(n log n)</b>. Worst: <b>O(n²)</b> (bad pivot every time, e.g. always picking the smallest / largest element).</li>
<li>Space: <b>O(log n)</b> recursion stack on average, <b>O(n)</b> in the worst case. In-place (no extra array).</li>
<li>In practice the fastest comparison sort due to good cache behavior and small constants.</li>
</ul>"	Info2::Sorting
"<b>Quick Sort</b> — What is the best choice for a good pivot, and why? (3 practical choices + why pivot choice matters)"	"<b>Ideal pivot:</b> the <i>median</i> of the sub-array → both partitions have size ≈ n/2 → recursion depth ≈ log n → total <b>O(n log n)</b>.
But finding the true median costs an extra O(n) per call → too expensive in practice.

<b>Three practical pivot strategies:</b>
<ol>
<li><b>Random pivot</b> — pick a uniformly random index in <code>[lo, hi]</code>. Defeats adversarial inputs (sorted / reverse-sorted arrays). Expected <b>O(n log n)</b>, worst case still possible but extremely unlikely.</li>
<li><b>Median-of-three</b> — take the median of <code>a[lo]</code>, <code>a[mid]</code>, <code>a[hi]</code>. Cheap (3 comparisons), usually much closer to the true median than any single element. This is what most real-world implementations use.</li>
<li><b>Middle element</b> <code>a[(lo+hi)/2]</code> — simple; avoids the O(n²) worst case on already-sorted input (the classic trap when using the first or last element).</li>
</ol>
<b>Why does pivot choice matter so much?</b>
<ul>
<li>A pivot near the median splits the array roughly in half → recursion depth ≈ <b>log n</b> → <b>O(n log n)</b> total work.</li>
<li>A pivot at the extreme (always min or max) splits into sizes 0 and n−1 → recursion depth ≈ <b>n</b> → <b>O(n²)</b> total.</li>
<li>So the pivot choice is exactly what separates the <i>average</i> case from the <i>worst</i> case in quicksort.</li>
</ul>"	Info2::Sorting
"<b>Heap Sort</b> — Explain what a <i>heap</i> is, how heap sort works (steps, ~5 bullets), code <code>heapify</code> and <code>heap_sort</code> in C, and state time &amp; space complexity (best / avg / worst, space)."	"<b>What is a heap?</b><br>
A <b>heap</b> is a <i>complete binary tree</i> (filled level-by-level, left-to-right) stored implicitly in an array. In a <b>max-heap</b>, every parent ≥ both its children (min-heap: parent ≤ children). For an element at index <code>i</code> (0-indexed):
<ul>
<li>Parent: <code>(i − 1) / 2</code></li>
<li>Left child: <code>2·i + 1</code></li>
<li>Right child: <code>2·i + 2</code></li>
</ul>
<b>Heap sort — steps:</b>
<ol>
<li><b>Build max-heap:</b> call <code>heapify</code> on every non-leaf node from index <code>n/2 − 1</code> down to <code>0</code>. Costs O(n).</li>
<li>The maximum element is now at index 0 (the root).</li>
<li>Swap <code>a[0]</code> with <code>a[i]</code> (the last element of the active heap) → max is placed in its final position.</li>
<li>Shrink the heap by 1 and call <code>heapify(a, i, 0)</code> to restore the max-heap property.</li>
<li>Repeat until the heap has 1 element. The array is now sorted ascending.</li>
</ol>
<b>C code:</b>
<pre><code>// Restore the max-heap property at node i,
// assuming the subtrees rooted at 2i+1 and 2i+2 are already valid heaps.
// n = size of the currently active heap.
void heapify(int a[], int n, int i) {
    // largest = index of the largest of {i, left child, right child}
    int largest = i;
    int l = 2*i + 1;   // left child
    int r = 2*i + 2;   // right child
    // compare with left child (only if it exists, i.e. l &lt; n)
    if (l &lt; n &amp;&amp; a[l] &gt; a[largest]) largest = l;
    // compare with right child (only if it exists)
    if (r &lt; n &amp;&amp; a[r] &gt; a[largest]) largest = r;
    // if a child was larger, swap and recurse down into that child
    if (largest != i) {
        int tmp = a[i]; a[i] = a[largest]; a[largest] = tmp;
        heapify(a, n, largest);
    }
}

void heap_sort(int a[], int n) {
    // ----- BUILD MAX-HEAP -----
    // Leaves (indices n/2 .. n-1) are trivially valid heaps on their own,
    // so we only need to heapify INTERNAL nodes.
    // We go bottom-up (last internal node → root) so that when heapify runs,
    // the children of i are already valid heaps. This is what makes
    // build-heap cost O(n) instead of O(n log n).
    for (int i = n/2 - 1; i &gt;= 0; i--) {
        heapify(a, n, i);
    }
    // ----- SORT -----
    // Repeatedly move the current max (root) to the end,
    // then restore the heap on the shrunken range [0, i).
    for (int i = n - 1; i &gt; 0; i--) {
        // move max (root) to position i (its final spot)
        int tmp = a[0]; a[0] = a[i]; a[i] = tmp;
        // active heap is now of size i; restore property at root
        heapify(a, i, 0);
    }
}</code></pre>
<b>Complexity:</b>
<ul>
<li>Time — Best / Average / Worst: <b>O(n log n)</b>. Build-heap is O(n), then n − 1 extractions cost O(log n) each.</li>
<li>Space: <b>O(1)</b> in-place (iterative <code>heapify</code>) or O(log n) recursion stack as written above.</li>
</ul>"	Info2::Sorting
"<b>Merge Sort</b> — Explain the <i>divide-and-conquer</i> principle (3 phases) and how merge sort applies it (steps, ~4 bullets), code it in C, and state time &amp; space complexity (best / avg / worst, space)."	"<b>Divide &amp; Conquer (3 phases):</b>
<ol>
<li><b>Divide</b> the problem into smaller sub-problems of the same type.</li>
<li><b>Conquer</b> each sub-problem recursively (base case: trivial size).</li>
<li><b>Combine</b> the sub-solutions into a solution of the original problem.</li>
</ol>
<b>Merge sort — steps:</b>
<ol>
<li>Base case: a sub-array of length ≤ 1 is already sorted → return.</li>
<li><b>Divide:</b> split the array at the middle index into two halves.</li>
<li><b>Conquer:</b> recursively merge-sort both halves.</li>
<li><b>Combine (merge):</b> walk two pointers through the two sorted halves and write the smaller current element into the output → produces one sorted array.</li>
</ol>
<b>C code:</b>
<pre><code>// Merge two already-sorted halves a[lo..mid] and a[mid+1..hi]
// into one sorted run stored back in a[lo..hi].
void merge(int a[], int lo, int mid, int hi) {
    // n1 = size of left half, n2 = size of right half
    int n1 = mid - lo + 1;
    int n2 = hi - mid;

    // create two temporary arrays and copy the two halves into them
    // (we need copies because we will overwrite a[lo..hi] while merging)
    int L[n1], R[n2];
    for (int i = 0; i &lt; n1; i++) L[i] = a[lo + i];
    for (int j = 0; j &lt; n2; j++) R[j] = a[mid + 1 + j];

    // i = read cursor in L, j = read cursor in R, k = write cursor in a
    int i = 0, j = 0, k = lo;

    // walk through BOTH halves at the same time
    while (i &lt; n1 &amp;&amp; j &lt; n2) {
        // pick whichever current element is smaller and copy it to a[k]
        if (L[i] &lt;= R[j]) a[k++] = L[i++];
        else              a[k++] = R[j++];
    }
    // at this point ONE of the two halves is fully consumed;
    // copy the leftover elements of the OTHER half (they are already sorted)
    while (i &lt; n1) a[k++] = L[i++];
    while (j &lt; n2) a[k++] = R[j++];
}

void merge_sort(int a[], int lo, int hi) {
    // base case: 0 or 1 elements → already sorted, nothing to do
    if (lo &lt; hi) {
        // DIVIDE: compute midpoint
        // (the form lo + (hi-lo)/2 avoids integer overflow vs. (lo+hi)/2)
        int mid = lo + (hi - lo) / 2;

        // CONQUER: recursively sort each half
        merge_sort(a, lo, mid);        // left half:  a[lo..mid]
        merge_sort(a, mid + 1, hi);    // right half: a[mid+1..hi]

        // COMBINE: merge the two sorted halves back together
        merge(a, lo, mid, hi);
    }
}</code></pre>
<b>Complexity:</b>
<ul>
<li>Time — Best / Average / Worst: <b>O(n log n)</b> (recurrence T(n) = 2·T(n/2) + Θ(n)).</li>
<li>Space: <b>O(n)</b> extra for the merge buffers + O(log n) recursion stack. <b>Not</b> in-place.</li>
</ul>"	Info2::Sorting
"<b>Dual-Pivot Quicksort</b> — In a dual-pivot quicksort implementation, an array <code>A[low...high]</code> is sorted based on two pivots <code>p1 = A[low]</code> and <code>p2 = A[high]</code>. The pivot <code>p1</code> must be less than <code>p2</code>, otherwise they have to be swapped. Here <code>low</code> is the left end of the array <code>A</code> and <code>high</code> is the right end of the array <code>A</code>.<br><br>
The partitioning process is the core of the dual-pivot quicksort. We begin partitioning the array into three proper partitions I, II and III, where:
<ul>
<li>Partition I: all elements are less than the left pivot <code>p1</code>.</li>
<li>Partition II: all elements are greater or equal to <code>p1</code> and less than the right pivot <code>p2</code>.</li>
<li>Partition III: all elements are greater than or equal to the right pivot <code>p2</code>.</li>
</ul>
After partitioning, the two pivots are placed in their proper final positions and the process is repeated recursively on each of the three partitions.<br><br>
Create a C program that implements dual-pivot quicksort to sort an array <code>A</code> of integers in increasing order. Your program should also print the array before and after sorting. For example, if the array is <code>[10, 7, 3, 15, 6, 2, 5, 1, 17, 8]</code>, the output should contain:
<pre><code>Before: [ 10 7 3 15 6 2 5 1 17 8 ]
After:  [ 1 2 3 5 6 7 8 10 15 17 ]</code></pre>"	"<b>Quick explanation in words:</b>
<ul>
<li>Choose two pivots: the leftmost and the rightmost element.</li>
<li>If they are in the wrong order, swap them so <code>p1 &lt;= p2</code>.</li>
<li>Then scan the array once and split it into three regions: smaller than <code>p1</code>, between the pivots, and greater or equal to <code>p2</code>.</li>
<li>Finally, put both pivots into their final places and recursively sort the three remaining parts.</li>
</ul>
<b>C code:</b>
<pre><code>#include &lt;stdio.h&gt;

#define ARRAY_SIZE 10

void swap(int A[], int i, int j) {
    // swap the two elements at indices i and j
    int tmp = A[i];
    A[i] = A[j];
    A[j] = tmp;
}

void printArray(int a[], int size) {
    // print the array in the required format: [ 1 2 3 ]
    printf("[");
    for (int i = 0; i &lt; size; i++) {
        printf(" %d", a[i]);
    }
    printf(" ]\\n");
}

void partitioning(int A[], int low, int high, int *p1, int *p2) {
    // A[low] is the left pivot, A[high] is the right pivot
    // l = first free position in partition I  (< p1)
    // k = current scanning index
    // g = first position of partition III (>= p2) from the right
    int l = low + 1;
    int k = low + 1;
    int g = high;

    // scan the array once and move each element into its correct partition
    while (k &lt; g) {
        // belongs to partition I: move it to the left side
        if (A[k] &lt; A[low]) {
            swap(A, l, k);
            l++;
            k++;
        }
        // belongs to partition III: move it to the right side
        else if (A[k] &gt;= A[high]) {
            g--;
            swap(A, k, g);
            // do NOT increment k here:
            // the element swapped in from the right has not been inspected yet
        }
        // otherwise it belongs to partition II and stays in the middle
        else {
            k++;
        }

        // prints intermediate partitioning steps
        printArray(A, ARRAY_SIZE);
    }

    // place the pivots into their final positions
    swap(A, low, l - 1);
    swap(A, high, k);

    // return the pivot positions through pointers
    *p1 = l - 1;
    *p2 = k;
}

void quicksort(int A[], int low, int high) {
    // base case: empty part or single element => already sorted
    if (high - low &lt;= 0) {
        return;
    }

    // ensure the left pivot is not larger than the right pivot
    if (A[low] &gt; A[high]) {
        swap(A, low, high);
    }

    int p1, p2;

    // partition the current subarray around the two pivots
    partitioning(A, low, high, &p1, &p2);

    // recursively sort the three partitions
    quicksort(A, low, p1 - 1);
    quicksort(A, p1 + 1, p2 - 1);
    quicksort(A, p2 + 1, high);
}

int main() {
    int A[] = {10, 7, 3, 15, 6, 2, 5, 1, 17, 8};
    int n = 10;

    printf("Before: ");
    printArray(A, n);

    quicksort(A, 0, n - 1);

    printf("After:  ");
    printArray(A, n);

    return 0;
}</code></pre>"	Info2::Sorting
"<b>Time complexity</b> — Rank the 8 most common time complexity classes from smallest (fastest) to largest (slowest)."	"<b>Ranking (smallest → largest):</b>
<pre><code>O(1) &lt; O(log n) &lt; O(n) &lt; O(n log n) &lt; O(n²) &lt; O(n³) &lt; O(2ⁿ) &lt; O(n!)</code></pre>
<b>What each one is:</b>
<ul>
<li><b>O(1)</b> — constant: same time regardless of n (array access, hash lookup).</li>
<li><b>O(log n)</b> — logarithmic: each step halves the problem (binary search).</li>
<li><b>O(n)</b> — linear: one pass through the input.</li>
<li><b>O(n log n)</b> — linearithmic: good comparison sorts (merge / heap / quick sort).</li>
<li><b>O(n²)</b> — quadratic: two nested loops (bubble / selection / insertion sort).</li>
<li><b>O(n³)</b> — cubic: e.g. naive matrix multiplication, Floyd–Warshall.</li>
<li><b>O(2ⁿ)</b> — exponential: subsets, naive recursive Fibonacci, brute-force SAT.</li>
<li><b>O(n!)</b> — factorial: all permutations, brute-force TSP.</li>
</ul>
<b>Rule of thumb:</b> any polynomial n^k grows slower than any exponential cⁿ, which grows slower than n!"	Info2::Complexity_and_Correctness
"<b>Time complexity</b> — What does <i>log n</i> mean in words when analyzing an algorithm? Why does it appear?"	"<b>In words:</b><br>
<code>log n</code> = the number of times you can <b>halve</b> n (or divide it by any constant factor &gt; 1) before reaching 1.
<ul>
<li>log₂ 1 024 = 10</li>
<li>log₂ 1 000 000 ≈ 20</li>
<li>log₂ 1 000 000 000 ≈ 30</li>
</ul>
Extremely slow growth!
<br>
<b>When it appears:</b><br>
An algorithm's complexity contains a <code>log n</code> factor whenever each step shrinks the problem to a <b>constant fraction</b> of its previous size — e.g. binary search halves the search range, balanced BST operations cut the depth in half, mergesort recurses on halves.
<br><br>
<b>Why no base?</b><br>
Changing the base of a log only multiplies by a constant (log₂ n = log₁₀ n / log₁₀ 2). Constants are dropped in O-notation → <code>O(log n)</code> is base-independent."	Info2::Complexity_and_Correctness
"<b>Time complexity</b> — What is the complexity of this code?
<pre><code>int first = a[0];
int last  = a[n - 1];
return first + last;</code></pre>"	"<b>O(1)</b> — constant.<br><br>
A fixed number of array accesses and one addition. No loop or recursion that depends on n → the work is independent of input size."	Info2::Complexity_and_Correctness
"<b>Time complexity</b> — What is the complexity of this code?
<pre><code>int count = 0;
for (int i = n; i &gt; 1; i = i / 2) {
    count++;
}</code></pre>"	"<b>O(log n)</b> — logarithmic.<br><br>
i is <b>halved</b> each iteration. The number of iterations until i drops from n down to 1 is log₂ n."	Info2::Complexity_and_Correctness
"<b>Time complexity</b> — What is the complexity of this code?
<pre><code>int sum = 0;
for (int i = 0; i &lt; n; i++) {
    sum += a[i];
}</code></pre>"	"<b>O(n)</b> — linear.<br><br>
A single pass through the array; the loop body is O(1) and runs exactly n times."	Info2::Complexity_and_Correctness
"<b>Time complexity</b> — What is the complexity of this code?
<pre><code>for (int i = 0; i &lt; n; i++) {
    for (int j = n; j &gt; 1; j = j / 2) {
        sum++;
    }
}</code></pre>"	"<b>O(n log n)</b> — linearithmic.<br><br>
Outer loop: n iterations.<br>
Inner loop: log n iterations (j is halved each step).<br>
Total: n · log n."	Info2::Complexity_and_Correctness
"<b>Time complexity</b> — What is the complexity of this code?
<pre><code>for (int i = 0; i &lt; n; i++) {
    for (int j = 0; j &lt; n; j++) {
        sum++;
    }
}</code></pre>"	"<b>O(n²)</b> — quadratic.<br><br>
Two nested loops, each running n times → n · n = n² body executions."	Info2::Complexity_and_Correctness
"<b>Time complexity</b> — What is the complexity of this code?
<pre><code>for (int i = 0; i &lt; n; i++) {
    for (int j = 0; j &lt; n; j++) {
        for (int k = 0; k &lt; n; k++) {
            sum++;
        }
    }
}</code></pre>"	"<b>O(n³)</b> — cubic.<br><br>
Three nested loops, each running n times → n · n · n = n³ body executions."	Info2::Complexity_and_Correctness
"<b>Time complexity</b> — What is the complexity of this code?
<pre><code>int fib(int n) {
    if (n &lt;= 1) return n;
    return fib(n - 1) + fib(n - 2);
}</code></pre>"	"<b>O(2ⁿ)</b> — exponential.<br><br>
Each call spawns 2 recursive calls and the recursion depth is ≈ n → the call tree has ≈ 2ⁿ nodes.<br>
(More precisely Θ(φⁿ) with φ ≈ 1.618 — the golden ratio — but in O-notation we say O(2ⁿ).)"	Info2::Complexity_and_Correctness
"<b>Time complexity</b> — What is the complexity of this code?
<pre><code>void permute(int a[], int l, int n) {
    if (l == n - 1) { print(a, n); return; }
    for (int i = l; i &lt; n; i++) {
        int tmp = a[l]; a[l] = a[i]; a[i] = tmp;
        permute(a, l + 1, n);
        tmp = a[l]; a[l] = a[i]; a[i] = tmp;
    }
}</code></pre>"	"<b>O(n!)</b> — factorial.<br><br>
Generates every permutation of the array. At depth k there are (n − k) remaining choices, so the total number of leaf calls equals n · (n−1) · ... · 1 = n!"	Info2::Complexity_and_Correctness
"<b>Correctness</b> — What is an <i>up-loop</i> vs a <i>down-loop</i>? Give a small example of each and say what the invariant looks like."	"<b>Up-loop</b> — the counter <b>increases</b> from a lower bound to an upper bound. The invariant typically describes the PREFIX <code>a[0..i-1]</code>.
<pre><code>for (int i = 0; i &lt; n; i++) {
    // invariant: a[0..i-1] already processed
}</code></pre>
<b>Down-loop</b> — the counter <b>decreases</b> from a higher bound to a lower bound. The invariant typically describes the SUFFIX <code>a[i+1..n-1]</code>.
<pre><code>for (int i = n - 1; i &gt;= 0; i--) {
    // invariant: a[i+1..n-1] already processed
}</code></pre>
Both are equally expressive — pick whichever direction makes the invariant easier to write. Heap sort's main loop is a down-loop because the sorted suffix grows from the right end of the array."	Info2::Complexity_and_Correctness
"<b>Correctness</b> — Define <i>precondition</i>, <i>postcondition</i>, and <i>assertion</i>. Then write them for an iterative <code>binary_search</code>."	"<b>Definitions:</b>
<ul>
<li><b>Precondition</b> — what must be TRUE <i>before</i> the algorithm runs. The assumption it relies on.</li>
<li><b>Postcondition</b> — what is guaranteed to be TRUE <i>after</i> the algorithm returns.</li>
<li><b>Assertion</b> — a statement that must be TRUE at a specific point <i>during</i> execution (in C: <code>assert(...)</code>).</li>
</ul>
<b>Example — binary_search:</b>
<pre><code>// PRECONDITION:
//   a != NULL, n &gt;= 0, and a[0..n-1] is sorted
//   in non-decreasing order.
int binary_search(int a[], int n, int x) {
    int lo = 0, hi = n - 1;
    while (lo &lt;= hi) {
        // ASSERTION: 0 &lt;= lo  and  hi &lt; n
        int mid = lo + (hi - lo) / 2;
        if (a[mid] == x) return mid;
        if (a[mid] &lt;  x) lo = mid + 1;
        else             hi = mid - 1;
    }
    return -1;
}
// POSTCONDITION:
//   returns an index i with a[i] == x,
//   or -1 if x does not occur in a.</code></pre>"	Info2::Complexity_and_Correctness
"<b>Correctness</b> — What is a <i>loop invariant</i>? Name the 3 things you must prove. Illustrate with binary_search."	"<b>Loop invariant:</b> a property that holds before the loop starts, stays true after every iteration, and helps prove correctness when the loop exits.
<br><br>
<b>3 things to prove:</b>
<ol>
<li><b>Initialization:</b> the invariant holds <i>before</i> the first iteration.</li>
<li><b>Maintenance:</b> if it holds at the START of an iteration, it still holds at the END (so it holds before the next iteration).</li>
<li><b>Termination:</b> when the loop exits, the invariant combined with the loop's exit condition implies the postcondition. (And the loop must actually terminate — usually a strictly decreasing &quot;variant&quot; proves this.)</li>
</ol>
<b>Binary search — invariant:</b> &quot;If x occurs in a, then x is in <code>a[lo..hi]</code>.&quot;
<ul>
<li><b>Initialization:</b> lo = 0, hi = n − 1 → <code>a[lo..hi]</code> is the entire array. Trivially holds.</li>
<li><b>Maintenance:</b> if a[mid] &lt; x, x can only be in <code>a[mid+1..hi]</code> → set lo = mid + 1 (symmetric for &gt;). Invariant preserved.</li>
<li><b>Termination:</b> loop ends when lo &gt; hi → <code>a[lo..hi]</code> is empty. By the invariant, x is not in a → return −1. (The variant <code>hi − lo</code> strictly decreases each iteration → termination guaranteed.)</li>
</ul>"	Info2::Complexity_and_Correctness
"<b>Testing</b> — Name 7 typical special / edge cases to consider when testing an algorithm (and why each one matters)."	"<ol>
<li><b>Empty array (n = 0):</b> does the code handle 0-length input without crashing or returning garbage?</li>
<li><b>One element (n = 1):</b> smallest non-empty case; often the base case of recursions or the boundary of loops.</li>
<li><b>Full / very large array:</b> stresses recursion depth, integer overflow, and quadratic blow-ups that don't show on small inputs.</li>
<li><b>Already sorted:</b> best case for some algorithms (insertion sort), but the classic <b>worst case</b> for quicksort with first/last pivot.</li>
<li><b>Reverse-sorted:</b> worst case for many &quot;optimistic&quot; algorithms; tests that swaps and comparisons work in the hardest direction.</li>
<li><b>Duplicates / all elements equal:</b> does partitioning still terminate? Does the algorithm still return a correct result when many keys tie?</li>
<li><b>Negative values (and zero):</b> sign assumptions can silently break correctness — e.g. counting sort and direct-address indexing don't handle negatives.</li>
</ol>"	Info2::Complexity_and_Correctness
"<b>Asymptotic complexity</b> — Define <b>O</b>, <b>Ω</b>, and <b>Θ</b>. What does each one express?"	"For functions f(n), g(n) &gt; 0 as n → ∞:
<br><br>
<b>Big O — upper bound:</b><br>
f(n) ∈ O(g(n)) ⇔ ∃ c &gt; 0, n₀ ≥ 0 such that <b>f(n) ≤ c · g(n)  ∀ n ≥ n₀.</b><br>
<i>&quot;f grows AT MOST as fast as g.&quot;</i>
<br><br>
<b>Big Omega — lower bound:</b><br>
f(n) ∈ Ω(g(n)) ⇔ ∃ c &gt; 0, n₀ ≥ 0 such that <b>f(n) ≥ c · g(n)  ∀ n ≥ n₀.</b><br>
<i>&quot;f grows AT LEAST as fast as g.&quot;</i>
<br><br>
<b>Big Theta — tight bound:</b><br>
f(n) ∈ Θ(g(n)) ⇔ f(n) ∈ O(g(n))  <b>AND</b>  f(n) ∈ Ω(g(n)).<br>
<i>&quot;f grows EXACTLY as fast as g (up to constant factors).&quot;</i>
<br><br>
<b>Cheat sheet:</b>
<ul>
<li>O  ↔  ≤   (upper bound)</li>
<li>Ω  ↔  ≥   (lower bound)</li>
<li>Θ  ↔  =   (tight bound)</li>
</ul>"	Info2::Complexity_and_Correctness
"<b>Asymptotic complexity</b> — Prove or disprove:
<ol>
<li>2^(n+1) = O(2ⁿ)</li>
<li>(2²)ⁿ = O(2ⁿ)</li>
<li>2 · n^(1.5) = O(n log n)</li>
</ol>"	"<b>1. 2^(n+1) = O(2ⁿ) — TRUE ✓</b><br>
2^(n+1) = 2 · 2ⁿ. Choose c = 2, n₀ = 0. Then 2^(n+1) ≤ 2 · 2ⁿ holds for all n ≥ 0. <b>QED</b>
<br><br>
<b>2. (2²)ⁿ = O(2ⁿ) — FALSE ✗</b><br>
(2²)ⁿ = 4ⁿ. Look at the ratio:<br>
4ⁿ / 2ⁿ = (4/2)ⁿ = 2ⁿ → ∞ as n → ∞.<br>
So NO constant c can make 4ⁿ ≤ c · 2ⁿ hold for all large n. <b>QED</b>
<br><br>
<b>3. 2 · n^(1.5) = O(n log n) — FALSE ✗</b><br>
Ratio:<br>
n^(1.5) / (n log n) = √n / log n → ∞ as n → ∞.<br>
(Every positive power of n eventually beats every polylog.) So no constant c works. <b>QED</b>
<br><br>
<b>Key reminders:</b>
<ul>
<li>a · cⁿ = O(cⁿ) — constants in the <b>coefficient</b> don't matter.</li>
<li>aⁿ ≠ O(bⁿ) when a &gt; b — the <b>base</b> of an exponential matters.</li>
<li>n^ε (any ε &gt; 0) eventually beats every polylog.</li>
</ul>"	Info2::Complexity_and_Correctness
"<b>Asymptotic complexity</b> — Determine the Θ(·) of each formula and justify briefly. (3 standard + 2 tricky)
<ol>
<li>n^k + cⁿ      (k constant, c &gt; 1)</li>
<li>log^k n + n^ε  (ε &gt; 0)</li>
<li>80·n² log n + 5·n² + n</li>
<li>log(n!)</li>
<li>2^(log₂ n) + n</li>
</ol>"	"<b>1.</b> n^k + cⁿ  →  <b>Θ(cⁿ)</b><br>
Any exponential beats any polynomial: cⁿ ≫ n^k for large n. The polynomial is the lower-order term and is absorbed.
<br><br>
<b>2.</b> log^k n + n^ε  →  <b>Θ(n^ε)</b><br>
Any positive power of n grows faster than any polylog (polynomial in log n). So n^ε dominates.
<br><br>
<b>3.</b> 80·n² log n + 5·n² + n  →  <b>Θ(n² log n)</b><br>
Drop constants and keep the highest-order term. Ranking: n² log n ≻ n² ≻ n.
<br><br>
<b>4.</b> log(n!)  →  <b>Θ(n log n)</b><br>
By Stirling: n! ≈ √(2πn) · (n/e)ⁿ, so log(n!) ≈ n log n − n + ½ log n + O(1). Dominant term: n log n. <i>(This is exactly why comparison sorting has the Ω(n log n) lower bound.)</i>
<br><br>
<b>5.</b> 2^(log₂ n) + n  →  <b>Θ(n)</b><br>
By definition of the logarithm: 2^(log₂ n) = n. So the whole expression is n + n = 2n.<br>
<i>Watch out:</i> 2^(log n) only equals n when the log's base is 2.
<br><br>
<b>Cheat sheet — dominance ladder (left grows slower):</b><br>
1 ≪ log n ≪ log² n ≪ √n ≪ n ≪ n log n ≪ n² ≪ n³ ≪ 2ⁿ ≪ 3ⁿ ≪ n! ≪ nⁿ"	Info2::Complexity_and_Correctness
"<b>Asymptotic complexity</b> — Calculate the asymptotic tight bound for the following functions and rank them by order of growth (lowest first). Work it out step by step.
<ol>
<li><code>f₁(n) = 10 lg(20n⁴) + 8229n³ + 20231n² + 128n log n</code></li>
<li><code>f₂(n) = log₂(n) + 50√n + log n</code></li>
<li><code>f₃(n) = 14400</code></li>
<li><code>f₄(n) = 2 log(6 log n²) + log(πn²) + 4(log n)²</code></li>
<li><code>f₅(n) = (2n + 3)!</code></li>
</ol>"	"<b>Step-by-step simplification:</b>
<br><br>
<b>1. f₁(n)</b>
<pre><code>f₁(n) = 10 lg(20n⁴) + 8229n³ + 20231n² + 128n log n

10 lg(20n⁴)
= 10 [lg 20 + lg(n⁴)]
= 10 [constant + 4 lg n]
= Θ(log n)</code></pre>
So
<pre><code>f₁(n) = Θ(log n) + Θ(n³) + Θ(n²) + Θ(n log n)</code></pre>
Dominant term: <code>n³</code>
<pre><code>f₁(n) = Θ(n³)</code></pre>

<b>2. f₂(n)</b>
<pre><code>f₂(n) = log₂(n) + 50√n + log n
      = Θ(log n) + Θ(√n) + Θ(log n)</code></pre>
Since <code>√n</code> grows faster than <code>log n</code>,
<pre><code>f₂(n) = Θ(√n)</code></pre>

<b>3. f₃(n)</b>
<pre><code>f₃(n) = 14400 = constant</code></pre>
Therefore
<pre><code>f₃(n) = Θ(1)</code></pre>

<b>4. f₄(n)</b>
<pre><code>f₄(n) = 2 log(6 log n²) + log(πn²) + 4(log n)²</code></pre>
First term:
<pre><code>2 log(6 log n²)
= 2 log(6 · 2 log n)
= 2 log(12 log n)
= Θ(log log n)</code></pre>
Second term:
<pre><code>log(πn²)
= log π + log(n²)
= constant + 2 log n
= Θ(log n)</code></pre>
Third term:
<pre><code>4(log n)² = Θ(log² n)</code></pre>
So
<pre><code>f₄(n) = Θ(log log n) + Θ(log n) + Θ(log² n)</code></pre>
Dominant term: <code>log² n</code>
<pre><code>f₄(n) = Θ(log² n)</code></pre>

<b>5. f₅(n)</b>
<pre><code>f₅(n) = (2n + 3)!</code></pre>
Shifting or multiplying the argument by a constant does not change the fact that this is factorial growth.
<pre><code>f₅(n) = Θ((2n + 3)!)</code></pre>
This grows much faster than any polynomial or polylogarithm.

<b>Ranking by growth (lowest first):</b>
<pre><code>f₃(n) &lt; f₄(n) &lt; f₂(n) &lt; f₁(n) &lt; f₅(n)</code></pre>
or equivalently
<pre><code>Θ(1) &lt; Θ(log² n) &lt; Θ(√n) &lt; Θ(n³) &lt; Θ((2n + 3)!)</code></pre>

<b>Quick comparison facts used:</b>
<pre><code>1 &lt; log n &lt; log² n &lt; √n &lt; n &lt; n log n &lt; n² &lt; n³ &lt; ... &lt; n! </code></pre>"	Info2::Complexity_and_Correctness
"<b>Algorithm design</b> — Given an array A of n integers and an integer c, decide whether any TWO elements of A sum to c.<br>
(a) Write <code>pairSum</code> for the general case in C.<br>
(b) If A is sorted (non-decreasing), write a faster <code>pairSumSorted</code> in C.<br>
(c) Give the worst-case complexity of each."	"<b>(a) pairSum — generic O(n²) version (check every pair):</b>
<pre><code>int pairSum(int A[], int n, int c) {
    // try every unordered pair (i, j) with i &lt; j
    for (int i = 0; i &lt; n - 1; i++) {
        for (int j = i + 1; j &lt; n; j++) {
            if (A[i] + A[j] == c) return 1;   // found a pair
        }
    }
    return 0;                                  // no such pair
}</code></pre>
<b>(b) pairSumSorted — two-pointer O(n) version (exploits sortedness):</b><br>
Idea: place a pointer at the smallest element (lo) and another at the largest (hi). If their sum is TOO SMALL, the only way to make it larger is to move lo to the right (a bigger value). If TOO LARGE, move hi to the left.
<pre><code>int pairSumSorted(int A[], int n, int c) {
    int lo = 0, hi = n - 1;
    while (lo &lt; hi) {
        int sum = A[lo] + A[hi];
        if (sum == c) return 1;
        if (sum &lt;  c) lo++;     // need a larger sum
        else          hi--;     // need a smaller sum
    }
    return 0;
}</code></pre>
<b>(c) Worst-case complexity (input sorted):</b>
<ul>
<li><b>pairSum: O(n²)</b> — examines every unordered pair, regardless of sortedness.</li>
<li><b>pairSumSorted: O(n)</b> — each iteration advances exactly one of the two pointers; together they can move at most n − 1 steps before meeting.</li>
</ul>
<b>Correctness of the two-pointer trick:</b> we never miss a solution because at the boundary, moving lo right OR hi left changes the sum in only ONE direction — and the values we &quot;skip&quot; were already proven not to be part of any solution."	Info2::Complexity_and_Correctness
"<b>Recurrences</b> — How does repeated substitution work in principle? Solve <code>T(n) = 2T(n/2) + c n</code> with repeated substitution. (4 steps + final result)"	"<b>Repeated substitution: 4 steps</b>
<ol>
<li><b>Expand once or twice</b> so you see the pattern.</li>
<li><b>Write the i-th expansion</b> with a general exponent / coefficient.</li>
<li><b>Stop at the base case</b> by solving for i.</li>
<li><b>Simplify</b> the final expression and keep the dominant term.</li>
</ol>
<b>Example:</b>
<pre><code>T(n) = 2T(n/2) + c n

= 2[ T(n/4) + c(n/2) ] + c n
= 2² T(n/4) + 2 c n

= 2²[ T(n/8) + c(n/4) ] + 2 c n
= 2³ T(n/8) + 3 c n

After i steps:
T(n) = 2^i T(n / 2^i) + i c n</code></pre>
Now stop when <code>n / 2^i = 1</code>. Then <code>2^i = n</code>, so <code>i = log₂ n</code>.
<pre><code>T(n) = 2^(log₂ n) T(1) + c n log₂ n
     = n T(1) + c n log₂ n</code></pre>
If <code>T(1) = d</code> is a constant, then
<pre><code>T(n) = d n + c n log n = Θ(n log n)</code></pre>
<b>Key idea:</b> each level contributes another <code>c n</code>, and there are <code>log n</code> levels."	Info2::Recurrences
"<b>Recurrences</b> — Practice: state the recurrence (including the base case) for this algorithm.
<pre><code>int mystery(int A[], int lo, int hi) {
    if (lo == hi) return A[lo];
    int mid = lo + (hi - lo) / 2;
    int left  = mystery(A, lo, mid);
    int right = mystery(A, mid + 1, hi);
    int sum = 0;
    for (int i = lo; i &lt;= hi; i++) {
        sum += A[i];
    }
    return left + right + sum;
}</code></pre>"	"<b>Recurrence:</b>
<pre><code>T(1) = c
T(n) = 2T(n/2) + c n</code></pre>
<b>Why?</b>
<ul>
<li><b>2T(n/2)</b> because the function recursively solves the left half and the right half.</li>
<li><b>+ c n</b> because after the two recursive calls it runs one loop over the whole current subarray.</li>
<li><b>T(1) = c</b> because when only one element is left, the function does constant work and returns immediately.</li>
</ul>
<b>General rule:</b> when writing a recurrence, count:
<ol>
<li>How many recursive calls?</li>
<li>What size does each subproblem have?</li>
<li>How much non-recursive work happens outside the calls?</li>
</ol>"	Info2::Recurrences
"<b>Recurrences</b> — How does substitution with guessing work? Solve <code>T(n) = 4T(n/2) + n</code> with guessing + induction, and explain how to make good guesses. (4 steps + guess tips + result)"	"<b>Substitution with guessing: 4 steps</b>
<ol>
<li><b>Guess</b> the asymptotic answer.</li>
<li><b>Assume</b> the guess is true for smaller inputs.</li>
<li><b>Substitute</b> that assumption into the recurrence.</li>
<li><b>Choose constants</b> so the induction closes.</li>
</ol>
<b>How to make a good guess:</b>
<ul>
<li>Compare with <code>n^(log_b a)</code> if the recurrence has the form <code>aT(n/b)+f(n)</code>.</li>
<li>Expand the first 2-3 levels and look for a pattern.</li>
<li>Use a recurrence tree mentally: does the work per level grow, stay equal, or shrink?</li>
<li>Start with the dominant term and ignore lower-order details at first.</li>
</ul>
<b>Example:</b> <code>T(n) = 4T(n/2) + n</code><br>
Here <code>a = 4</code>, <code>b = 2</code>, so <code>n^(log_b a) = n²</code>. Good guess: <code>T(n) = Θ(n²)</code>.
<br><br>
<b>Upper bound O(n²):</b><br>
Assume for all smaller inputs that <code>T(m) ≤ C m² - m</code>.
Then
<pre><code>T(n) = 4T(n/2) + n
     ≤ 4[ C(n/2)² - n/2 ] + n
     = C n² - n</code></pre>
So <code>T(n) ≤ C n²</code>.
<br><br>
<b>Lower bound Ω(n²):</b><br>
Assume for all smaller inputs that <code>T(m) ≥ c m²</code>.
Then
<pre><code>T(n) = 4T(n/2) + n
     ≥ 4 c (n/2)² + n
     = c n² + n
     ≥ c n²</code></pre>
So <code>T(n) ∈ Ω(n²)</code>.
<br><br>
Since both bounds hold:
<pre><code>T(n) = Θ(n²)</code></pre>"	Info2::Recurrences
"<b>Recurrences</b> — How do you solve a recurrence with a recurrence tree? Solve <code>T(n) = T(n/4) + T(n/2) + n²</code>. (root, first layer, second layer, general layer, result)"	"<b>Recurrence-tree idea:</b> write the work done on each layer, then sum over all layers.
<br><br>
<b>Root:</b>
<pre><code>cost = n²</code></pre>
<b>First layer: 2 nodes</b>
<pre><code>T(n/4) contributes (n/4)² = n²/16
T(n/2) contributes (n/2)² = n²/4
Layer total = n²/16 + n²/4 = 5n²/16</code></pre>
<b>Second layer: 4 nodes</b>
The two children split again into sizes
<pre><code>n/16, n/8, n/8, n/4</code></pre>
so the cost on that layer is
<pre><code>(n/16)² + (n/8)² + (n/8)² + (n/4)²
= n²/256 + n²/64 + n²/64 + n²/16
= 25n²/256
= (5/16)² n²</code></pre>
<b>Pattern:</b><br>
Each layer is multiplied by <code>5/16</code>, because
<pre><code>(1/4)² + (1/2)² = 1/16 + 1/4 = 5/16</code></pre>
So the i-th layer costs
<pre><code>(5/16)^i n²</code></pre>
Total:
<pre><code>T(n) = n² [ 1 + 5/16 + (5/16)² + ... ]</code></pre>
This is a geometric series with ratio <code>5/16 &lt; 1</code>, so it converges to a constant.
Therefore
<pre><code>T(n) = Θ(n²)</code></pre>
<b>Key intuition:</b> the root already dominates, because the total work shrinks by the constant factor <code>5/16</code> from one level to the next."	Info2::Recurrences
"<b>Recurrences</b> — How does the master method work? State the 3 cases, say when the extra check is needed, explain what <code>T(1)</code> means, and solve <code>T(n) = 4T(n/2) + n² log³ n</code>. (2 parameters, 3 cases, 1 extra check, result)"	"<b>Master method setup:</b><br>
For recurrences of the form
<pre><code>T(n) = a T(n/b) + f(n)</code></pre>
<ul>
<li><code>a</code> = number of recursive calls</li>
<li><code>b</code> = factor by which the input size shrinks</li>
<li><code>f(n)</code> = non-recursive work done at one node</li>
</ul>
Compute the comparison term
<pre><code>n^(log_b a)</code></pre>
Then compare <code>f(n)</code> with that term.
<br><br>
<b>Case 1:</b> <code>f(n) = O(n^(log_b a - ε))</code> for some <code>ε &gt; 0</code><br>
Then
<pre><code>T(n) = Θ(n^(log_b a))</code></pre>
<b>Case 2:</b> <code>f(n) = Θ(n^(log_b a) log^k n)</code> for some <code>k ≥ 0</code><br>
Then
<pre><code>T(n) = Θ(n^(log_b a) log^(k+1) n)</code></pre>
<b>Case 3:</b> <code>f(n) = Ω(n^(log_b a + ε))</code> for some <code>ε &gt; 0</code><br>
and additionally
<pre><code>a f(n/b) ≤ c f(n)</code></pre>
for some constant <code>c &lt; 1</code> and all sufficiently large <code>n</code>.<br>
Then
<pre><code>T(n) = Θ(f(n))</code></pre>
<b>Extra check:</b> that regularity condition is needed in <b>Case 3</b>.
<br><br>
<b>What does T(1) mean?</b><br>
It is the running time of the <b>base case</b>: when the input size is 1, recursion stops and only constant work remains. In the recursion tree it is the cost stored in each leaf.
<br><br>
<b>Example:</b> <code>T(n) = 4T(n/2) + n² log³ n</code><br>
Here
<pre><code>a = 4,  b = 2
n^(log_b a) = n^(log₂ 4) = n²</code></pre>
Also
<pre><code>f(n) = n² log³ n = Θ( n^(log_b a) log³ n )</code></pre>
So this is <b>Case 2</b> with <code>k = 3</code>.
Therefore
<pre><code>T(n) = Θ(n² log^4 n)</code></pre>"	Info2::Recurrences
"<b>Recurrences</b> — The maximum-subarray algorithm finds the contiguous subarray that has the largest sum within an unsorted array <code>A</code> with <code>n</code> integers. For example, for array <code>A = [-2, -5, 6, -2, -3, 1, 5]</code>, the maximum subarray is <code>[6, -2, -3, 1, 5]</code>.<br><br>
The algorithm works as follows:<br>
Firstly, it divides the input array into two equal partitions: I (<code>A[0]...A[mid]</code>) and II (<code>A[mid+1]...A[n-1]</code>). Afterwards, it calls itself recursively on both partitions to find the maximum subarray of each partition. The combination step decides the maximum-subarray by comparing three arrays: the maximum-subarray from the left part, the maximum-subarray from the right part, and the maximum-subarray that overlaps the middle. The maximum-subarray that overlaps the middle is determined by considering all elements to the left and all elements to the right of the middle.<br><br>
2.2 Provide C code for the maximum-subarray algorithm.<br>
2.3 What is the recurrence relation of the algorithm and its asymptotic tight bound?"	"<b>Idea in simple words:</b>
<ul>
<li>Split the array into a left half and a right half.</li>
<li>Recursively compute the best subarray fully inside the left half.</li>
<li>Recursively compute the best subarray fully inside the right half.</li>
<li>Also compute the best subarray that <b>crosses the middle</b>: it must end at <code>mid</code> on the left side and start at <code>mid+1</code> on the right side.</li>
<li>The overall answer is the best of those three candidates.</li>
</ul>
<b>C code:</b>
<pre><code>int maxOverlapArraySum(int arr[], int l, int m, int r) {
    // sum = running sum while we scan away from the middle
    // left_sum / right_sum = best sums found on each side
    int sum = 0;
    int left_sum = -50000;   // acts like -infinity

    // start at the middle and extend to the LEFT
    // this finds the best subarray that ENDS at index m
    for (int i = m; i >= l; i--) {
        sum = sum + arr[i];
        if (sum > left_sum) {
            left_sum = sum;
        }
    }

    // reset sum before scanning the right side
    sum = 0;
    int right_sum = -50000;  // acts like -infinity

    // start just right of the middle and extend to the RIGHT
    // this finds the best subarray that STARTS at index m+1
    for (int i = m + 1; i <= r; i++) {
        sum = sum + arr[i];
        if (sum > right_sum) {
            right_sum = sum;
        }
    }

    // the best crossing subarray must use both the best left part
    // and the best right part
    return left_sum + right_sum;
}

int maxSubArraySum(int arr[], int l, int r) {
    // base case: with one element, the best subarray is the element itself
    if (l == r) {
        return arr[l];
    }

    // split current interval into two halves
    int m = (l + r) / 2;

    // recursively solve left half, right half, and crossing case
    int leftArraySum = maxSubArraySum(arr, l, m);
    int rightArraySum = maxSubArraySum(arr, m + 1, r);
    int overlapArraySum = maxOverlapArraySum(arr, l, m, r);

    // return the largest of the three candidates
    if (leftArraySum >= rightArraySum && leftArraySum >= overlapArraySum) {
        return leftArraySum;
    } else if (rightArraySum >= leftArraySum && rightArraySum >= overlapArraySum) {
        return rightArraySum;
    }
    return overlapArraySum;
}

// initial call:
// int max_sum = maxSubArraySum(arr, 0, n - 1);</code></pre>
<b>Recurrence:</b>
<pre><code>T(1) = Θ(1)
T(n) = 2T(n/2) + Θ(n)</code></pre>
<b>Why this recurrence?</b>
<ul>
<li><code>2T(n/2)</code> because the algorithm recursively solves the left half and the right half.</li>
<li><code>+ Θ(n)</code> because the crossing subarray is computed by one scan to the left and one scan to the right of the middle, together linear in the current subproblem size.</li>
</ul>
<b>Step-by-step derivation of the bound:</b>
<pre><code>T(n) = 2T(n/2) + c n</code></pre>
Use the Master Method:
<pre><code>a = 2,  b = 2
n^(log_b a) = n^(log₂ 2) = n</code></pre>
Also
<pre><code>f(n) = c n = Θ(n)</code></pre>
So
<pre><code>f(n) = Θ(n^(log_b a))</code></pre>
This is <b>Master Method Case 2</b> with <code>k = 0</code>, hence
<pre><code>T(n) = Θ(n log n)</code></pre>
<b>Final answer:</b>
<pre><code>Recurrence: T(n) = 2T(n/2) + Θ(n)
Asymptotic tight bound: Θ(n log n)</code></pre>"	Info2::Recurrences
"<b>Recurrences</b> — Write a recursive function that reduces an integer array by summing neighbouring elements, so that the rows form a pyramid. Explain the idea simply before the code. (idea + code)"	"<b>Simple idea:</b>
<ul>
<li>From an array of length <code>n</code>, build a new array of length <code>n-1</code>.</li>
<li>The new element at index <code>i</code> is the sum of its two neighbours below it: <code>A[i] + A[i+1]</code>.</li>
<li>Repeat recursively until the array becomes empty.</li>
<li>Print <b>after</b> the recursive call, so the smallest row appears first and the pyramid is printed from top to bottom.</li>
</ul>
<b>C code:</b>
<pre><code>void draw_pyramid(const int A[], int n, int level) {
    // base case: nothing to print / reduce
    if (n &lt; 1) {
        return;
    }

    // recursive case: build reduced array of length n-1, then recurse
    int t[n - 1];
    for (int i = 0; i &lt; n - 1; i++) {
        t[i] = A[i] + A[i + 1];
    }

    draw_pyramid(t, n - 1, level + 1);

    // print AFTER recursion so that top of pyramid appears first (top-down)
    print_level(A, n, level);
}</code></pre>
<b>Why does printing happen after recursion?</b><br>
If you printed first, you would print the bottom row first. By printing on the way back up, the smallest reduced row is printed first, then the larger ones underneath it."	Info2::Recurrences
"<b>Pointers</b> — Fill in the blanks. (All in one card)<br><br>
A pointer is a variable that stores the <b>[what?]</b> of another variable.<br>
If <code>int x = 7;</code>, then <code>&amp;x</code> means the <b>[what?]</b> of <code>x</code>.<br>
If <code>int *p = &amp;x;</code>, then <code>p</code> stores the <b>[what?]</b> of <code>x</code>.<br>
The notation <code>*p</code> means the <b>[what?]</b> stored at the address inside <code>p</code>.<br>
The type <code>int *</code> means: <b>[what kind of thing?]</b> to <code>int</code>.<br>
The symbol used to declare a pointer is <b>[which symbol?]</b>.<br>
If <code>struct node *p;</code>, then <code>p-&gt;a</code> means the same as <b>[equivalent expression?]</b>.<br>
Explain briefly how address vs. pointer vs. value-at-address differ."	"<b>Completed text:</b><br><br>
A pointer is a variable that stores the <b>address</b> of another variable.<br>
If <code>int x = 7;</code>, then <code>&amp;x</code> means the <b>address</b> of <code>x</code>.<br>
If <code>int *p = &amp;x;</code>, then <code>p</code> stores the <b>address</b> of <code>x</code>.<br>
The notation <code>*p</code> means the <b>value</b> stored at the address inside <code>p</code>.<br>
The type <code>int *</code> means: <b>pointer</b> to <code>int</code>.<br>
The symbol used to declare a pointer is <b>*</b>.<br>
If <code>struct node *p;</code>, then <code>p-&gt;a</code> means the same as <b><code>(*p).a</code></b>.<br><br>
<b>Address vs. pointer vs. value-at-address:</b>
<ul>
<li><b>Address:</b> the memory location of a variable, e.g. <code>&amp;x</code>.</li>
<li><b>Pointer:</b> a variable whose content is such an address, e.g. <code>p</code>.</li>
<li><b>Value at address:</b> the actual object stored there, accessed by dereferencing, e.g. <code>*p</code>.</li>
</ul>
<b>Mini example:</b>
<pre><code>int x = 7;
int *p = &amp;x;

// &amp;x  = address of x
// p   = same address
// *p  = 7</code></pre>"	Info2::Abstract_Data_Types
"<b>Pointers</b> — How do double pointers work? Explain <code>int **p</code>, <code>**p</code>, and why double pointers are useful in code. In particular, explain the case where a tree node itself may change."	"<b>Core idea:</b><br>
A <b>double pointer</b> stores the address of a pointer.
<pre><code>int x = 20;
int *q = &amp;x;    // q points to x
int **p = &amp;q;   // p points to q</code></pre>
Then:
<ul>
<li><code>q</code> is an address of an <code>int</code>.</li>
<li><code>*q</code> is the integer value stored there.</li>
<li><code>p</code> is an address of a pointer.</li>
<li><code>*p</code> is the pointer <code>q</code>.</li>
<li><code>**p</code> is the integer value behind <code>q</code>, so here <code>**p = 20</code>.</li>
</ul>
<b>Small example:</b>
<pre><code>int x = 20;
int *q = &amp;x;
int **p = &amp;q;

printf("%d\\n", **p);   // 20
**p = 99;               // changes x through two dereferences</code></pre>
<b>Why useful?</b>
<ul>
<li>Whenever a function must be able to change a pointer owned by the caller.</li>
<li>Typical cases: inserting into a linked list, changing the root of a tree, allocating memory and storing the new pointer back.</li>
</ul>
<b>Tree example:</b><br>
Suppose a tree is represented by a pointer to its root:
<pre><code>struct node {
    int key;
    struct node *left, *right;
};</code></pre>
If a function may need to create the root node when the tree is empty, then it must be able to change the caller's root pointer itself:
<pre><code>void insert_root_if_empty(struct node **root, int x) {
    if (*root == NULL) {
        *root = malloc(sizeof(struct node));
        (*root)-&gt;key = x;
        (*root)-&gt;left = NULL;
        (*root)-&gt;right = NULL;
    }
}</code></pre>
Why <code>struct node **root</code>? Because:
<ul>
<li><code>root</code> = address of the caller's root pointer</li>
<li><code>*root</code> = the actual root pointer</li>
<li><code>**root</code> would be the actual node data if you dereference further</li>
</ul>
If you passed only <code>struct node *root</code>, the function would receive a copy of the pointer and could not replace the caller's root."	Info2::Abstract_Data_Types
"<b>Linked List</b> — What is the structure of a singly linked list in C, and how do you access one specific value?"	"<b>Structure:</b>
<pre><code>struct node {
    int value;
    struct node *next;
};

struct node *root;</code></pre>
Each node stores:
<ul>
<li>a data field (<code>value</code>)</li>
<li>a pointer to the next node (<code>next</code>)</li>
</ul>
The list itself is usually represented by a pointer to the first node:
<pre><code>struct node *root = NULL;</code></pre>
<b>Access one specific value:</b><br>
You cannot jump directly to position <code>i</code> like in an array. You must walk node by node.
<pre><code>int get_at(struct node *root, int index) {
    struct node *cur = root;

    // move index steps forward
    for (int i = 0; i &lt; index; i++) {
        if (cur == NULL) return -1;   // index out of bounds
        cur = cur-&gt;next;
    }

    if (cur == NULL) return -1;       // index out of bounds
    return cur-&gt;value;
}</code></pre>
<b>Key point:</b> accessing index <code>i</code> costs <b>O(i)</b>, because you must follow the links one by one."	Info2::Abstract_Data_Types
"<b>Linked List</b> — How do you populate a linked list with items? Code: create a linked list by adding <code>88</code>, <code>52</code>, and <code>12</code>."	"<b>One direct way:</b> allocate the nodes one after another and connect them step by step.
<pre><code>#include &lt;stdlib.h&gt;

struct node {
    int value;
    struct node *next;
};

struct node *root;
struct node *p;

root = malloc(sizeof(struct node));
root-&gt;value = 88;

root-&gt;next = malloc(sizeof(struct node));
p = root-&gt;next;
p-&gt;value = 52;

p-&gt;next = malloc(sizeof(struct node));
p = p-&gt;next;
p-&gt;value = 12;

p-&gt;next = NULL;</code></pre>
After this, the list is:
<pre><code>root -> [88] -> [52] -> [12] -> NULL</code></pre>"	Info2::Abstract_Data_Types
"<b>Linked List</b> — How do you count the number of occurrences of an element <code>x</code> in a linked list?"	"<pre><code>struct node {
    int value;
    struct node *next;
};

int count_occurrences(struct node *head, int x) {
    int count = 0;
    struct node *cur = head;

    // walk through the whole list
    while (cur != NULL) {
        if (cur-&gt;value == x) {
            count++;
        }
        cur = cur-&gt;next;
    }

    return count;
}</code></pre>
<b>Idea:</b> scan the whole list and increment a counter whenever the current node contains <code>x</code>.<br>
<b>Complexity:</b> <code>O(n)</code> time, <code>O(1)</code> extra space."	Info2::Abstract_Data_Types
"<b>Linked List</b> — How do you insert an item at the beginning of a linked list?"	"<pre><code>#include &lt;stdlib.h&gt;

struct node {
    int value;
    struct node *next;
};

void insert_front(struct node **head, int x) {
    struct node *new_node = malloc(sizeof(struct node));

    // fill the new node
    new_node-&gt;value = x;

    // new node points to the old first node
    new_node-&gt;next = *head;

    // head now becomes the new node
    *head = new_node;
}</code></pre>
<b>Why use <code>struct node **head</code>?</b><br>
Because inserting at the front changes the head pointer itself.<br>
<b>Complexity:</b> <code>O(1)</code>."	Info2::Abstract_Data_Types
"<b>Linked List</b> — How do you append a list <code>L1</code> to a linked list <code>L2</code>? (After the operation, <code>L2</code> should be followed by <code>L1</code>.)"	"<pre><code>struct node {
    int value;
    struct node *next;
};

void append_list(struct node **L2, struct node *L1) {
    // if L2 is empty, the result is just L1
    if (*L2 == NULL) {
        *L2 = L1;
        return;
    }

    // find the last node of L2
    struct node *cur = *L2;
    while (cur-&gt;next != NULL) {
        cur = cur-&gt;next;
    }

    // connect the last node of L2 with the first node of L1
    cur-&gt;next = L1;
}</code></pre>
<b>Idea:</b> walk to the end of <code>L2</code>, then set its last <code>next</code> pointer to the head of <code>L1</code>.<br>
<b>Complexity:</b> <code>O(length(L2))</code> if only the head of <code>L2</code> is known."	Info2::Abstract_Data_Types
"<b>Linked List</b> — How do you insert a number at the end of a linked list?"	"<pre><code>#include &lt;stdlib.h&gt;

struct node {
    int value;
    struct node *next;
};

void insert_end(struct node **head, int x) {
    struct node *new_node = malloc(sizeof(struct node));
    new_node-&gt;value = x;
    new_node-&gt;next = NULL;

    // empty list: new node becomes the first node
    if (*head == NULL) {
        *head = new_node;
        return;
    }

    // otherwise walk to the last node
    struct node *cur = *head;
    while (cur-&gt;next != NULL) {
        cur = cur-&gt;next;
    }

    // link the new node at the end
    cur-&gt;next = new_node;
}</code></pre>
<b>Complexity:</b> <code>O(n)</code> if only the head is stored. With an extra tail pointer it can be <code>O(1)</code>."	Info2::Abstract_Data_Types
"<b>Linked List</b> — How do you delete an element from a linked list? (Delete the first occurrence of <code>x</code>.)"	"<pre><code>#include &lt;stdlib.h&gt;

struct node {
    int value;
    struct node *next;
};

void delete_first(struct node **head, int x) {
    struct node *cur = *head;
    struct node *prev = NULL;

    // search for the first node whose value is x
    while (cur != NULL &amp;&amp; cur-&gt;value != x) {
        prev = cur;
        cur = cur-&gt;next;
    }

    // x not found => nothing to delete
    if (cur == NULL) {
        return;
    }

    // special case: delete the first node
    if (prev == NULL) {
        *head = cur-&gt;next;
    } else {
        prev-&gt;next = cur-&gt;next;
    }

    free(cur);
}</code></pre>
<b>Key idea:</b> in a singly linked list you must know the predecessor node, because deletion is done by bypassing the current node."	Info2::Abstract_Data_Types
"<b>Linked List</b> — How do you delete an entire linked list?"	"<pre><code>#include &lt;stdlib.h&gt;

struct node {
    int value;
    struct node *next;
};

void delete_list(struct node **head) {
    struct node *cur = *head;

    while (cur != NULL) {
        struct node *next = cur-&gt;next;   // save next before freeing cur
        free(cur);
        cur = next;
    }

    // after deleting all nodes, the list is empty
    *head = NULL;
}</code></pre>
<b>Why set <code>*head = NULL</code>?</b><br>
So the caller does not keep a dangling pointer to freed memory.<br>
<b>Complexity:</b> <code>O(n)</code>."	Info2::Abstract_Data_Types
"<b>Linked List</b> — What are the costs of these linked-list operations? Assume a singly linked list with only a head pointer. (Insertion at beginning, insertion at end, <code>isEmpty?</code>, delete, print)"	"<b>Assumption:</b> singly linked list, only head pointer known, no tail pointer, delete means “delete a value by searching for it”.<br><br>
<ul>
<li><b>Insertion at beginning:</b> <code>O(1)</code> — just create a node and change <code>head</code>.</li>
<li><b>Insertion at end:</b> <code>O(n)</code> — must first walk to the last node.</li>
<li><b><code>isEmpty?</code>:</b> <code>O(1)</code> — just test whether <code>head == NULL</code>.</li>
<li><b>Delete:</b> <code>O(n)</code> — usually must search for the node and its predecessor.</li>
<li><b>Print:</b> <code>O(n)</code> — must visit every node once.</li>
</ul>
<b>Important note:</b> if you also store a tail pointer, insertion at end becomes <code>O(1)</code>."	Info2::Abstract_Data_Types
"<b>Linked List</b> — Doubly linked list: what is the structure in C, and what is the advantage over a singly linked list?"	"<b>Structure:</b>
<pre><code>struct dnode {
    int value;
    struct dnode *prev;
    struct dnode *next;
};

struct dnode *root;</code></pre>
Each node stores:
<ul>
<li>its value</li>
<li>a pointer to the previous node</li>
<li>a pointer to the next node</li>
</ul>
<b>Main advantage:</b><br>
Insertion and deletion take <b>O(1)</b> <b>if the node is already known</b>, because you do not need to search for its predecessor.
<br><br>
<b>Why exactly?</b><br>
In a singly linked list, if you want to delete node <code>cur</code>, you must know the node before it in order to change its <code>next</code> pointer. That predecessor is not directly stored, so you often need an extra search.<br><br>
In a doubly linked list, <code>cur-&gt;prev</code> gives the predecessor immediately, so you can splice <code>cur</code> out in constant time:
<pre><code>cur-&gt;prev-&gt;next = cur-&gt;next;
cur-&gt;next-&gt;prev = cur-&gt;prev;</code></pre>
The same idea makes inserting before or after a known node <code>O(1)</code> as well.
<br><br>
<b>Trade-off:</b> doubly linked lists need more memory and more pointer updates."	Info2::Abstract_Data_Types
"<b>Stack</b> — What is the stack principle? How does a stack work? Name 5 operations you can do with a stack."	"<b>Principle:</b> <b>LIFO</b> = <b>L</b>ast <b>I</b>n, <b>F</b>irst <b>O</b>ut.<br>
The most recently inserted element is the first one removed.
<br><br>
<b>How it works:</b>
<ul>
<li>Insertion happens only at the <b>top</b>.</li>
<li>Removal also happens only at the <b>top</b>.</li>
<li>You never insert or remove in the middle.</li>
</ul>
<b>Five common operations:</b>
<ol>
<li><b>create / init</b> — make an empty stack</li>
<li><b>push(x)</b> — put <code>x</code> on top</li>
<li><b>pop()</b> — remove the top element</li>
<li><b>top() / peek()</b> — read the top element without removing it</li>
<li><b>isEmpty()</b> — check whether the stack is empty</li>
</ol>"	Info2::Abstract_Data_Types
"<b>Stack</b> — Why do we often work with pointers or a sentinel first dummy element in C when implementing a stack?"	"<b>Main reason:</b> in C, function arguments are passed <b>by value</b>.<br>
So if the stack is represented by a pointer to the first node and you pass only that pointer into a function, the function receives a <b>copy</b> of the pointer.
<br><br>
Example:
<pre><code>void push(struct node *top, int x) {
    // changing top here changes only the local copy
}</code></pre>
If <code>push</code> needs to make a new first node, then the caller's top pointer must change too. Therefore you usually use either:
<ul>
<li><b>a pointer to the pointer</b>, e.g. <code>struct node **top</code></li>
<li><b>or a stack struct / sentinel node</b> whose address stays fixed</li>
</ul>
<b>Why a sentinel helps:</b><br>
A sentinel (dummy first node) gives you a stable object whose address never changes. The actual first real element is stored in <code>sentinel-&gt;next</code>. Then functions can modify <code>sentinel-&gt;next</code> without having to replace the sentinel itself.
<br><br>
<b>Summary:</b>
<ul>
<li>Need to change the top pointer itself? Use <code>struct node **top</code>.</li>
<li>Want a fixed front object? Use a sentinel / wrapper struct.</li>
</ul>"	Info2::Abstract_Data_Types
"<b>Stack</b> — How do I create a stack, and how do I push and pop an element in C?"	"<pre><code>#include &lt;stdlib.h&gt;

struct node {
    int value;
    struct node *next;
};

// create an empty stack
struct node *create_stack(void) {
    return NULL;
}

void push(struct node **top, int x) {
    struct node *new_node = malloc(sizeof(struct node));

    // new node becomes the new top
    new_node-&gt;value = x;
    new_node-&gt;next = *top;
    *top = new_node;
}

int pop(struct node **top) {
    // caller should ensure the stack is not empty
    struct node *tmp = *top;
    int x = tmp-&gt;value;

    // remove the top node
    *top = tmp-&gt;next;
    free(tmp);

    return x;
}</code></pre>
<b>Usage:</b>
<pre><code>struct node *S = create_stack();
push(&amp;S, 10);
push(&amp;S, 20);
int x = pop(&amp;S);   // x = 20</code></pre>
<b>Why this works:</b> both <code>push</code> and <code>pop</code> may change the top pointer, so they receive <code>struct node **top</code>."	Info2::Abstract_Data_Types
"<b>Queue</b> — What is the queue principle? What happens at the head and the tail? Explain circular indexing."	"<b>Principle:</b> <b>FIFO</b> = <b>F</b>irst <b>I</b>n, <b>F</b>irst <b>O</b>ut.<br>
The oldest inserted element is removed first.
<br><br>
<b>Head vs. tail:</b>
<ul>
<li><b>enqueue</b> inserts at the <b>tail</b></li>
<li><b>dequeue</b> removes from the <b>head</b></li>
</ul>
<b>Circular indexing:</b><br>
In an array-based queue, after reaching the last array position you do not stop. You wrap around to index <code>0</code>.
<pre><code>next_index = (current_index + 1) % capacity</code></pre>
This makes the array behave like a circle.
<br><br>
Example with capacity 5:
<pre><code>0 1 2 3 4
        ^ tail

tail = (4 + 1) % 5 = 0</code></pre>
So after index 4, the next free position is again index 0.
<br><br>
<b>Why useful?</b><br>
It avoids shifting all elements after each dequeue. Head and tail simply move around the ring."	Info2::Abstract_Data_Types
"<b>Queue</b> — What is the structure of a queue in C? Write <code>enqueue</code> and <code>dequeue</code> in C."	"<pre><code>#define CAPACITY 100

typedef struct {
    int a[CAPACITY];
    int head;   // index of the first element
    int tail;   // index where the next inserted element goes
    int size;   // current number of stored elements
} Queue;

void initQueue(Queue *q) {
    q-&gt;head = 0;
    q-&gt;tail = 0;
    q-&gt;size = 0;
}

void enqueue(Queue *q, int x) {
    // assume the queue is not full
    q-&gt;a[q-&gt;tail] = x;                  // insert at tail
    q-&gt;tail = (q-&gt;tail + 1) % CAPACITY; // move tail circularly
    q-&gt;size++;
}

int dequeue(Queue *q) {
    // assume the queue is not empty
    int x = q-&gt;a[q-&gt;head];              // remove from head
    q-&gt;head = (q-&gt;head + 1) % CAPACITY; // move head circularly
    q-&gt;size--;
    return x;
}</code></pre>
<b>Why store <code>size</code>?</b><br>
It makes checking empty/full easier:
<ul>
<li>empty iff <code>size == 0</code></li>
<li>full iff <code>size == CAPACITY</code></li>
</ul>"	Info2::Abstract_Data_Types
"<b>Queue/Stack Exercise — reverseEven</b> — Provide pseudocode for <code>reverseEven(Q)</code>, which reverses the order of the even integers in a queue while keeping the odd integers in their original positions. Only use the Queue and Stack ADT operations. Analyze the complexity for arbitrary values, all even values, and all odd values."	"<b>Goal:</b><br>
Given:
<pre><code>Q = [14, 17, 16, 18, 21, 7, 28, 40]</code></pre>
we want:
<pre><code>Q = [40, 17, 28, 18, 21, 7, 16, 14]</code></pre>
Only the even values are reversed. The odd values stay at the same positions.
<br><br>
<b>Idea:</b>
<ol>
<li>First scan the queue once. Every even value is pushed onto a stack. Since a stack is LIFO, popping later gives the even values in reverse order.</li>
<li>During that first scan, enqueue every value again, so the queue returns to its original order.</li>
<li>Second scan: if the current queue value is even, discard it and enqueue the next value from the stack. If it is odd, enqueue it unchanged.</li>
</ol>
<b>Pseudocode:</b>
<pre><code>Algorithm reverseEven(Q)
    // S will store all even values temporarily.
    // Because S is a stack, the last even value pushed will be popped first.
    S = initStack()

    // Store the original queue length.
    // Important: we must loop exactly n times, because Q is modified during the loop.
    n = queueSize(Q)

    // ----- first pass: collect even values -----
    // After this pass, Q has the same order as before,
    // and S contains all even values in encounter order.
    for i = 1 to n do
        // Remove the front element so we can inspect it.
        val = deQueue(Q)

        // Check whether val is even.
        // Even values are exactly the values whose order must be reversed.
        if val % 2 == 0 then
            // Push the even value onto S.
            // Later, pop(S) will return the even values in reverse order.
            push(S, val)

        // Put val back at the rear of Q.
        // After n such rotations, Q is back in its original order.
        enQueue(Q, val)

    // ----- second pass: rebuild Q -----
    // We again process each original position exactly once.
    for i = 1 to n do
        // Remove the current front value.
        // This tells us whether the current position was even or odd.
        val = deQueue(Q)

        // If this position originally contained an even value,
        // replace it with the next reversed even value.
        if val % 2 == 0 then
            // pop(S) gives the even values from last to first.
            enQueue(Q, pop(S))
        else
            // Odd values must stay at the same positions,
            // so we enqueue the same odd value again.
            enQueue(Q, val)</code></pre>
<b>Complexity:</b><br>
Let <code>n = queueSize(Q)</code> and let <code>e</code> be the number of even values.
<ul>
<li>Queue operations: first pass has <code>n</code> dequeues and <code>n</code> enqueues; second pass also has <code>n</code> dequeues and <code>n</code> enqueues.</li>
<li>Stack operations: each even value is pushed once and popped once, so <code>2e</code> stack operations.</li>
<li>Total primitive ADT operations: <code>4n + 2e</code>.</li>
</ul>
Because <code>0 ≤ e ≤ n</code>, the runtime is always <b><code>O(n)</code></b>.
<br><br>
<b>Cases:</b>
<ul>
<li><b>No assumption:</b> worst case <code>e = n</code> → <code>4n + 2n = 6n</code> operations → <code>O(n)</code>.</li>
<li><b>All values even:</b> <code>e = n</code> → <code>O(6n) = O(n)</code>.</li>
<li><b>All values odd:</b> <code>e = 0</code> → <code>O(4n) = O(n)</code>.</li>
</ul>
<b>Space:</b> <code>O(e)</code>, worst case <code>O(n)</code>, for the stack of even values."	Info2::Abstract_Data_Types
"<b>Stack Exercise — sortStack</b> — Provide pseudocode for <code>sortStack(S)</code>, which sorts a stack of integers in ascending order so that the smallest integer is at the top. Only use the Stack ADT operations <code>initStack</code>, <code>push</code>, <code>pop</code>, and <code>isEmpty</code>."	"<b>Idea:</b><br>
We repeatedly find the maximum remaining value and push it back onto <code>S</code>.
<br><br>
This sounds backwards, but it gives the correct final stack:
<ul>
<li>First, the largest value is pushed onto empty <code>S</code>.</li>
<li>Then the second-largest is pushed on top of it.</li>
<li>At the end, the smallest value is pushed last, so it is on top.</li>
</ul>
<b>Auxiliary stacks:</b>
<ul>
<li><code>a</code>: holds the values that are not yet sorted.</li>
<li><code>b</code>: temporary stack for values that are not the current maximum.</li>
<li><code>S</code>: rebuilt into sorted order.</li>
</ul>
<b>Pseudocode:</b>
<pre><code>Algorithm sortStack(S)
    // a stores the values that are still unsorted.
    a = initStack()

    // b is a temporary stack used while searching for the maximum.
    b = initStack()

    // Move all values from S into a.
    // This empties S so we can rebuild it in sorted order.
    while not isEmpty(S) do
        // pop(S) removes the current top of S.
        // push(a, ...) stores it in the unsorted auxiliary stack.
        push(a, pop(S))

    // Each outer-loop iteration chooses one maximum from a.
    // That maximum is then pushed onto S.
    while not isEmpty(a) do
        // Start by assuming the first popped value is the maximum.
        maxV = pop(a)

        // Scan all other unsorted values in a.
        while not isEmpty(a) do
            // Take one candidate value from a.
            tmpV = pop(a)

            // If tmpV is larger, it becomes the new maximum.
            if tmpV &gt; maxV then
                // The old maxV is not the maximum anymore,
                // so keep it in b for a later round.
                push(b, maxV)

                // tmpV is now the best maximum found so far.
                maxV = tmpV
            else
                // tmpV is not the maximum of this round,
                // so keep it in b for a later round.
                push(b, tmpV)

        // maxV is the largest remaining value.
        // Push it onto S as the next sorted value.
        push(S, maxV)

        // Now b contains all values except maxV.
        // Swap a and b so the next round scans those remaining values.
        c = a
        a = b
        b = c</code></pre>
<b>Correctness intuition:</b><br>
Every round removes the largest remaining value and pushes it onto <code>S</code>. Since larger values are pushed earlier and smaller values later, the smallest value ends up at the top.
<br><br>
<b>Complexity:</b>
<ul>
<li>First round scans <code>n</code> values, next round <code>n-1</code>, then <code>n-2</code>, ...</li>
<li>Total time: <code>O(n + (n-1) + ... + 1) = O(n²)</code>.</li>
<li>Extra space: <code>O(n)</code> for the auxiliary stacks.</li>
</ul>"	Info2::Abstract_Data_Types
"<b>Stack Exercise — kth Largest</b> — A stack <code>S</code> contains <code>n</code> unique integers. Provide pseudocode for <code>Kth(S, n, k)</code> that returns the <code>k</code>-th largest value. You may change <code>S</code> and use one auxiliary stack, but no other composite data structure."	"<b>Idea:</b><br>
Find and remove the maximum value <code>k</code> times.
<ul>
<li>After the first scan, the largest value is removed.</li>
<li>After the second scan, the second-largest value is removed.</li>
<li>After the <code>k</code>-th scan, the removed value is the <code>k</code>-th largest.</li>
</ul>
Because all values are unique, we can safely remove the found maximum by checking <code>top != x</code>.
<br><br>
<b>Variables:</b>
<ul>
<li><code>S</code>: input stack, changed by the algorithm.</li>
<li><code>T</code>: auxiliary stack used to temporarily hold values during a scan.</li>
<li><code>x</code>: maximum value found in the current round.</li>
<li><code>top</code>: value just popped from a stack.</li>
</ul>
<b>Pseudocode:</b>
<pre><code>Algorithm Kth(S, n, k)
    // T is the only auxiliary stack.
    // It temporarily stores values while we scan S.
    T = initStack()

    // Repeat k times.
    // Round 1 removes the largest, round 2 the second-largest, etc.
    for i = 1 to k do
        // Pop one value to initialize the current maximum candidate.
        top = pop(S)

        // Store it in T so it is not lost.
        push(T, top)

        // x is the largest value found in this round so far.
        x = top

        // Scan all remaining values in S.
        while not isEmpty(S) do
            // Remove one value from S.
            top = pop(S)

            // Save it in T while we continue scanning.
            push(T, top)

            // Update x if this value is larger.
            if top &gt; x then
                x = top

        // Move all values back to S except x.
        // This deletes the current maximum from S permanently.
        while not isEmpty(T) do
            // Get one saved value back from T.
            top = pop(T)

            // Since all values are unique, only the found maximum equals x.
            if top != x then
                // Keep every value that is not the current maximum.
                push(S, top)

    // After k rounds, x is the value removed in round k,
    // therefore it is the kth largest value.
    return x</code></pre>
<b>Complexity:</b>
<ul>
<li>Round 1 scans <code>n</code> values, round 2 scans <code>n-1</code>, ..., round <code>k</code> scans about <code>n-k+1</code> values.</li>
<li>Time: <code>O(kn)</code>; worst case <code>k = n</code> gives <code>O(n²)</code>.</li>
<li>Extra space: <code>O(n)</code> for the auxiliary stack.</li>
</ul>"	Info2::Abstract_Data_Types
"<b>Ordered List</b> — What is the difference between <code>struct node *</code> and <code>struct node **</code>? What does this allow?"	"<ul>
<li><code>struct node *</code> = a pointer to one node</li>
<li><code>struct node **</code> = a pointer to a pointer to a node</li>
</ul>
<b>Meaning:</b>
<ul>
<li>If a function gets <code>struct node *p</code>, it can access and modify the node that <code>p</code> points to.</li>
<li>If a function gets <code>struct node **p</code>, it can also modify the pointer itself.</li>
</ul>
<b>What does that allow?</b><br>
It lets a function change the head of a list.
That is exactly what you need when:
<ul>
<li>initializing an empty list</li>
<li>inserting a new smallest element at the front</li>
<li>deleting the first element</li>
</ul>
<b>Example:</b>
<pre><code>void insert_front(struct node **head, int x) {
    // this function can change *head itself
}</code></pre>"	Info2::Abstract_Data_Types
"<b>Ordered List</b> — C coding: initialize an ordered list in C with <code>struct node ** init()</code>."	"<pre><code>#include &lt;stdlib.h&gt;

struct node {
    int value;
    struct node *next;
};

struct node **init(void) {
    // allocate memory for the head pointer itself
    struct node **L = malloc(sizeof(struct node *));

    // initially the list is empty
    *L = NULL;

    return L;
}</code></pre>
<b>Why return <code>struct node **</code>?</b><br>
Because the ordered-list ADT stores a modifiable head pointer. Functions working on the ADT can then change <code>*L</code>, i.e. the first real node of the list."	Info2::Abstract_Data_Types
"<b>Ordered List</b> — How do you retrieve the first element of an ordered list?"	"<pre><code>struct node {
    int value;
    struct node *next;
};

int first(struct node **L) {
    // assume the list is not empty
    return (*L)-&gt;value;
}</code></pre>
<b>Why is this correct?</b><br>
In an ordered list, the first node stores the smallest element if the order is ascending.<br>
If the list may be empty, you first need a check:
<pre><code>if (*L == NULL) { /* error / special value */ }</code></pre>"	Info2::Abstract_Data_Types
"<b>Ordered List</b> — How do you insert a value iteratively into an ordered list?"	"<pre><code>#include &lt;stdlib.h&gt;

struct node {
    int value;
    struct node *next;
};

void insert_ordered(struct node **L, int x) {
    struct node *new_node = malloc(sizeof(struct node));
    new_node-&gt;value = x;
    new_node-&gt;next = NULL;

    // special case: empty list or x belongs before the first element
    if (*L == NULL || x &lt; (*L)-&gt;value) {
        new_node-&gt;next = *L;
        *L = new_node;
        return;
    }

    // walk until cur points to the node after which x should be inserted
    struct node *cur = *L;
    while (cur-&gt;next != NULL &amp;&amp; cur-&gt;next-&gt;value &lt; x) {
        cur = cur-&gt;next;
    }

    // insert new_node between cur and cur->next
    new_node-&gt;next = cur-&gt;next;
    cur-&gt;next = new_node;
}</code></pre>
<b>Idea:</b> move forward until you find the first place where the next value is not smaller than <code>x</code>, then splice the new node into that gap.<br>
<b>Complexity:</b> <code>O(n)</code>."	Info2::Abstract_Data_Types
"<b>BT vs BST</b> — What is the difference between a binary tree and a binary search tree? Also: what is a min-heap, and how is it different from a BST?"	"<b>Binary tree (BT):</b> every node has at most 2 children (left, right). <i>No</i> constraint on the values.
<br><br>
<b>Binary search tree (BST):</b> a binary tree with the BST property:
<ul>
<li>All values in the LEFT subtree of a node are <b>&lt;</b> the node's value</li>
<li>All values in the RIGHT subtree are <b>&gt;</b> the node's value</li>
<li>The property holds <b>recursively</b> for every node</li>
</ul>
<b>Min-heap:</b>
<ul>
<li>A <b>complete binary tree</b> (filled level by level, left to right), usually stored implicitly in an array.</li>
<li><b>Heap property:</b> every parent ≤ both its children (max-heap: parent ≥ children).</li>
</ul>
<b>Min-heap vs BST — key differences:</b>
<ul>
<li>In a min-heap, only the <b>root</b> is guaranteed to be the global minimum. There is <b>no order between siblings</b>, and the left subtree is <b>not</b> &quot;all smaller&quot; than the right.</li>
<li>In a BST, in-order traversal gives the values in <b>sorted order</b>. In a heap, no traversal yields sorted order.</li>
<li>Use heaps for <b>priority queues</b> (fast min/max + insert).</li>
<li>Use BSTs for <b>ordered search</b>, range queries, sorted iteration.</li>
</ul>"	Info2::Binary_Trees_BST
"<b>BST</b> — What are the complexities of search, insert, and delete in a BST? (average and worst case)"	"All three operations walk <b>one path</b> from the root downward, so they cost <b>O(h)</b> where <code>h</code> is the height of the tree.
<br><br>
<table style=&quot;border-collapse: collapse;&quot;>
<tr><th style=&quot;text-align:left; padding-right:18px;&quot;>Operation</th><th style=&quot;text-align:left; padding-right:18px;&quot;>Average</th><th style=&quot;text-align:left;&quot;>Worst</th></tr>
<tr><td>search</td><td><b>O(log n)</b></td><td><b>O(n)</b></td></tr>
<tr><td>insert</td><td><b>O(log n)</b></td><td><b>O(n)</b></td></tr>
<tr><td>delete</td><td><b>O(log n)</b></td><td><b>O(n)</b></td></tr>
</table>
<br>
<b>Why?</b>
<ul>
<li><b>Balanced / random tree:</b> height ≈ <code>log n</code> → <b>O(log n)</b>.</li>
<li><b>Degenerate tree</b> (e.g. inserting already-sorted values): the BST becomes a linked list → height ≈ <code>n</code> → <b>O(n)</b>.</li>
</ul>
This is exactly why self-balancing trees (AVL, Red-Black) exist: they guarantee <code>h = O(log n)</code> even in the worst case."	Info2::Binary_Trees_BST
"<b>Tree traversal</b> — How do <i>inorder</i>, <i>preorder</i>, and <i>postorder</i> walks work? Give the rule for each, and a small C function for each."	"For each traversal you do something at the node (<b>N</b>) and visit the left (<b>L</b>) and right (<b>R</b>) subtrees. The <b>order</b> in which N, L, R are processed is what differs:
<ul>
<li><b>Inorder</b> = L, N, R → visit left, then output node, then visit right. <i>For a BST this yields sorted order.</i></li>
<li><b>Preorder</b> = N, L, R → output node, then visit left, then visit right. <i>Useful for copying a tree, or printing a prefix expression.</i></li>
<li><b>Postorder</b> = L, R, N → visit left, then visit right, then output node. <i>Useful when children must be processed before the parent</i> (deleting a tree, computing height bottom-up).</li>
</ul>
<b>C code:</b>
<pre><code>struct node {
    int value;
    struct node *left;
    struct node *right;
};

void inorder(struct node *root) {
    if (root == NULL) return;
    inorder(root-&gt;left);
    printf(&quot;%d &quot;, root-&gt;value);
    inorder(root-&gt;right);
}

void preorder(struct node *root) {
    if (root == NULL) return;
    printf(&quot;%d &quot;, root-&gt;value);
    preorder(root-&gt;left);
    preorder(root-&gt;right);
}

void postorder(struct node *root) {
    if (root == NULL) return;
    postorder(root-&gt;left);
    postorder(root-&gt;right);
    printf(&quot;%d &quot;, root-&gt;value);
}</code></pre>
<b>Complexity:</b> <code>O(n)</code> for all three (every node is visited once)."	Info2::Binary_Trees_BST
"<b>BST</b> — With a BST, how do you get an ordered output (small to large)? With which traversal?"	"Use <b>INORDER</b> traversal.
<br><br>
<b>Why does it work?</b><br>
By the BST property:
<ul>
<li>All values in the left subtree are smaller than the root.</li>
<li>All values in the right subtree are larger than the root.</li>
</ul>
Inorder visits L → N → R, so it prints all smaller values first, then the node, then all larger values. Applied recursively, this gives <b>ascending order</b>.
<br><br>
<b>For descending order:</b> do a &quot;reverse inorder&quot; → R, N, L."	Info2::Binary_Trees_BST
"<b>BST Exam Exercise — Print Two BSTs Sorted</b> — Given two BSTs <code>T1</code> and <code>T2</code> with roots <code>r1</code> and <code>r2</code>, print all elements of both trees in ascending order. You may use one array large enough to hold all <code>n + m</code> elements, but no sorting algorithm."	"<b>Idea:</b><br>
For a BST, <b>inorder traversal</b> visits values in ascending order. So:
<ol>
<li>Write all values of <code>T1</code> into the array from left to right using inorder.</li>
<li>Write all values of <code>T2</code> into the array after that, also using inorder.</li>
<li>Now the array contains two sorted blocks. To print all values sorted overall, merge the two sorted blocks like in merge sort.</li>
</ol>
This uses the BST order property instead of a separate sorting algorithm.
<br><br>
<b>Tree node:</b>
<pre><code>struct TreeNode {
    int val;
    struct TreeNode *left;
    struct TreeNode *right;
};</code></pre>
<b>Inorder tree walk with position index:</b>
<pre><code>int a[N];   // large enough for n + m elements
int idx;    // next free position in a

void ITW(struct TreeNode *p) {
    // If p is NULL, the current subtree is empty.
    // Empty subtrees contribute no values to the array.
    if (p != NULL) {
        // First visit the left subtree.
        // In a BST, all values there are smaller than p-&gt;val.
        ITW(p-&gt;left);

        // Store the current node value at the next free array position.
        a[idx] = p-&gt;val;

        // Move idx to the next free array position.
        idx++;

        // Finally visit the right subtree.
        // In a BST, all values there are larger than p-&gt;val.
        ITW(p-&gt;right);
    }
}</code></pre>
<b>Full printing idea:</b>
<pre><code>void printTwoBSTsSorted(struct TreeNode *r1, int n,
                        struct TreeNode *r2, int m) {
    // Assume a is the global/helper array from above,
    // with enough space for all elements from both trees.
    // The first n slots will hold T1, the next m slots will hold T2.

    // Start writing T1 at index 0.
    idx = 0;

    // Inorder traversal writes T1 into a[0..n-1] in sorted order.
    ITW(r1);

    // Start writing T2 right after the block for T1.
    idx = n;

    // Inorder traversal writes T2 into a[n..n+m-1] in sorted order.
    ITW(r2);

    // i scans the first sorted block a[0..n-1].
    int i = 0;

    // j scans the second sorted block a[n..n+m-1].
    int j = n;

    // While both blocks still contain values,
    // print the smaller current value and advance that block's cursor.
    while (i &lt; n &amp;&amp; j &lt; n + m) {
        if (a[i] &lt;= a[j]) {
            // a[i] is the next smallest remaining value.
            print(a[i]);

            // Move to the next value in the first block.
            i++;
        } else {
            // a[j] is the next smallest remaining value.
            print(a[j]);

            // Move to the next value in the second block.
            j++;
        }
    }

    // If T2 is exhausted first, print the remaining values of T1.
    while (i &lt; n) {
        print(a[i]);
        i++;
    }

    // If T1 is exhausted first, print the remaining values of T2.
    while (j &lt; n + m) {
        print(a[j]);
        j++;
    }
}</code></pre>
<b>Alternative matching the lecture idea:</b><br>
If you only need to show how values are inserted into an array during traversal, use an inorder tree walk with an index and direction:
<pre><code>void ITW(struct TreeNode *p, int d) {
    // Only process non-empty subtrees.
    if (p != NULL) {
        // Visit all smaller values first.
        ITW(p-&gt;left, d);

        // Store the current node at the current array position.
        a[idx] = p-&gt;val;

        // Move the array position by d.
        // For normal left-to-right writing, d = 1.
        idx = idx + d;

        // Visit all larger values last.
        ITW(p-&gt;right, d);
    }
}</code></pre>
Here <code>d = 1</code> writes left-to-right. A direction value is useful when you want the same traversal routine to fill different parts of the array.
<br><br>
<b>Complexity:</b>
<ul>
<li>Each tree node is visited once: <code>O(n + m)</code>.</li>
<li>The merge pass is also <code>O(n + m)</code>.</li>
<li>Total time: <code>O(n + m)</code>.</li>
<li>Extra space: <code>O(n + m)</code> for the array.</li>
</ul>"	Info2::Binary_Trees_BST
"<b>BST</b> — Search for a value <code>x</code> in a BST in C. Write both the iterative and the recursive version."	"<b>Idea:</b> compare <code>x</code> with the current node's value:
<ul>
<li>equal → found</li>
<li>smaller → go left</li>
<li>larger → go right</li>
<li>NULL → not found</li>
</ul>
<pre><code>struct node {
    int value;
    struct node *left;
    struct node *right;
};

// recursive
struct node *bst_search(struct node *root, int x) {
    if (root == NULL || root-&gt;value == x) {
        return root;
    }
    if (x &lt; root-&gt;value) {
        return bst_search(root-&gt;left, x);
    }
    return bst_search(root-&gt;right, x);
}

// iterative
struct node *bst_search_iter(struct node *root, int x) {
    struct node *cur = root;
    while (cur != NULL &amp;&amp; cur-&gt;value != x) {
        if (x &lt; cur-&gt;value) {
            cur = cur-&gt;left;
        } else {
            cur = cur-&gt;right;
        }
    }
    return cur;   // NULL if x is not in the tree
}</code></pre>
<b>Complexity:</b> <code>O(h)</code> → average <code>O(log n)</code>, worst <code>O(n)</code>."	Info2::Binary_Trees_BST
"<b>BST</b> — How do you find the minimum of a BST? (conceptually, no code)"	"Walk down the <b>left spine</b> of the tree:
<ul>
<li>Start at the root.</li>
<li>Keep following the <code>left</code> pointer.</li>
<li>The first node whose <code>left</code> is NULL is the minimum.</li>
</ul>
<b>Why?</b> By the BST property, every left subtree contains only smaller values. So the smallest value in the entire tree must be the <b>leftmost</b> node reachable from the root.
<br><br>
<b>Maximum:</b> symmetric — walk down the <b>right</b> spine.
<br><br>
<b>Complexity:</b> <code>O(h)</code>."	Info2::Binary_Trees_BST
"<b>BST</b> — Code: count the number of occurrences of <code>x</code> in a BST. The function must return how many times <code>x</code> appears in the tree (so it must also handle duplicates correctly)."	"<b>Idea:</b>
<ul>
<li>If <code>x</code> is smaller than the current node, only the LEFT subtree can contain <code>x</code> → recurse left.</li>
<li>If <code>x</code> is larger than the current node, only the RIGHT subtree can contain <code>x</code> → recurse right.</li>
<li>If <code>x</code> equals the current node's value: count this match as <b>1</b>, and (since the BST may carry duplicates on either side) also continue searching in <b>both</b> subtrees.</li>
</ul>
<pre><code>struct node {
    int value;
    struct node *left;
    struct node *right;
};

int count_occurrences(struct node *root, int x) {
    // base case: empty subtree contains 0 occurrences
    if (root == NULL) return 0;

    // x lies strictly left of root → only the left subtree can hold x
    if (x &lt; root-&gt;value) {
        return count_occurrences(root-&gt;left, x);
    }

    // x lies strictly right of root → only the right subtree can hold x
    if (x &gt; root-&gt;value) {
        return count_occurrences(root-&gt;right, x);
    }

    // x == root-&gt;value : this node counts as 1.
    // duplicates may live in either subtree, so check both.
    return 1 + count_occurrences(root-&gt;left, x)
             + count_occurrences(root-&gt;right, x);
}</code></pre>
<b>Complexity:</b>
<ul>
<li><b>No duplicates of x:</b> <code>O(h)</code> — average <code>O(log n)</code>, worst <code>O(n)</code>.</li>
<li><b>With many duplicates of x:</b> up to <code>O(n)</code> because each match forces recursion into both subtrees.</li>
</ul>"	Info2::Binary_Trees_BST
"<b>BST</b> — What is the <i>successor</i> of a node? Name the 2 cases for finding the successor (conceptual). Then code it (no parent pointer — walk down from the root)."	"<b>Successor of x:</b> the node with the <b>smallest value that is still &gt; x's value</b> — equivalently, the node that comes <i>right after x</i> in an inorder traversal.
<br><br>
<b>2 cases:</b>
<ol>
<li><b>x has a right subtree</b> → the successor is the <b>minimum of the right subtree</b> (its leftmost node).</li>
<li><b>x has no right subtree</b> → walk <b>down from the root</b> toward x. Each time you go LEFT, remember the current node as a candidate successor (<code>succ</code>). The last such candidate when you reach x is the successor. If you never went left, x is the maximum and has no successor.</li>
</ol>
<b>C code:</b>
<pre><code>struct node {
    int value;
    struct node *left;
    struct node *right;
};

struct node *bst_min(struct node *root) {
    if (root == NULL) return NULL;
    while (root-&gt;left != NULL) {
        root = root-&gt;left;
    }
    return root;
}

// root = root of the whole tree, x = the node whose successor we want
struct node *successor(struct node *root, struct node *x) {
    // case 1: x has a right subtree → minimum of that right subtree
    if (x-&gt;right != NULL) {
        return bst_min(x-&gt;right);
    }

    // case 2: no right subtree → walk down from the root toward x,
    // remembering the latest node we went LEFT from.
    struct node *p    = root;
    struct node *succ = NULL;

    while (p != x) {
        if (x-&gt;value &lt; p-&gt;value) {
            // x lies in p's left subtree → p is a candidate successor
            succ = p;
            p = p-&gt;left;
        } else {
            // x lies in p's right subtree → p is NOT a candidate
            p = p-&gt;right;
        }
    }

    return succ;   // NULL if x was the maximum (we never went left)
}</code></pre>
<b>Why case 2 works:</b> the successor of x must be an ancestor of x for which x sits in its LEFT subtree (so x is smaller than that ancestor, and no smaller ancestor with x in its left subtree exists below it). Each time we go left on the way down, we record the current node — the last one recorded is exactly that lowest ancestor.
<br><br>
<b>Complexity:</b> <code>O(h)</code>."	Info2::Binary_Trees_BST
"<b>BST</b> — Code: how do you insert a <code>struct node *p</code> into a BST? Use a <i>one-step-delayed-pointer</i>. Explain the core idea first."	"<b>Core idea (one-step-delayed-pointer):</b>
<ul>
<li>Walk down the tree from the root with a current pointer <code>x</code>.</li>
<li>Keep a second pointer <code>y</code> that is always <b>one step behind</b> <code>x</code> — so <code>y</code> is the parent of <code>x</code>.</li>
<li>When <code>x</code> falls off the tree (becomes NULL), <code>y</code> is exactly the parent of the empty slot.</li>
<li>Attach <code>p</code> as the left or right child of <code>y</code>, depending on how <code>p-&gt;value</code> compares with <code>y-&gt;value</code>.</li>
<li>Return the (possibly new) root <code>r</code>.</li>
</ul>
<b>C code (matches the textbook signature: returns the new root):</b>
<pre><code>struct node {
    int value;
    struct node *left;
    struct node *right;
};

// p = new node to insert (with p-&gt;left = p-&gt;right = NULL)
// r = current root of the BST (may be NULL if the tree is empty)
// returns the (possibly new) root of the BST
struct node *BSTreeInsert(struct node *p, struct node *r) {

    struct node *y = NULL;   // y is one step delayed → will end up as p's parent
    struct node *x = r;      // x walks down from the root

    // walk down until x runs off the tree
    while (x != NULL) {
        y = x;   // remember x as the parent of the next step

        if (x-&gt;value &lt; p-&gt;value) {
            // new value belongs to the RIGHT of x → go right
            x = x-&gt;right;
        } else {
            // new value belongs to the LEFT of x → go left
            x = x-&gt;left;
        }
    }

    // attach p in the correct slot under y
    if (y == NULL) {
        // tree was empty → p becomes the new root
        r = p;
    } else if (y-&gt;value &lt; p-&gt;value) {
        // p is larger than y → right child of y
        y-&gt;right = p;
    } else {
        // p is smaller (or equal) than y → left child of y
        y-&gt;left = p;
    }

    return r;
}</code></pre>
<b>Typical call:</b> <code>root = BSTreeInsert(new_node, root);</code><br>
<b>Complexity:</b> <code>O(h)</code>."	Info2::Binary_Trees_BST
"<b>BST</b> — How do you delete a node in a BST? Explain the 3 cases (conceptual), then code: delete <code>struct node *x</code> from a BST."	"<b>3 cases</b> by the number of children of the node to delete:
<ol>
<li><b>x is a leaf</b> (no children) → simply remove x. Its parent's pointer to it becomes NULL.</li>
<li><b>x has exactly one child</b> → replace x with that child in its parent's pointer slot.</li>
<li><b>x has two children</b> → replace x's <i>value</i> with the value of its <b>inorder successor</b> (= minimum of x's right subtree). Then recursively delete that successor in the right subtree (the successor has at most one child, so this falls into case 1 or 2).</li>
</ol>
<b>C code (line-by-line comments, book style with <code>q = p; p = p-&gt;right; free(q)</code>):</b>
<pre><code>struct node {
    int value;
    struct node *left;
    struct node *right;
};

// helper: smallest node in a subtree (leftmost node)
struct node *bst_min(struct node *root) {
    // walk down the LEFT spine until there is no more left child
    while (root-&gt;left != NULL) root = root-&gt;left;
    return root;
}

// deletes the node with value x from the BST whose root pointer is *p.
// p is a pointer-to-pointer so we can OVERWRITE *p directly in place;
// this writes through to the caller's pointer (e.g. the parent's
// left/right slot, or the top-level root pointer).
// usage at the top level:   bst_delete(&amp;root, x);
void bst_delete(struct node **p, int x) {

    // 1) base case: x is not in the tree → nothing to do
    if (*p == NULL) return;

    // 2) navigate: same comparisons as in BST search
    if (x &lt; (*p)-&gt;value) {
        // x must be in the LEFT subtree (BST property)
        // recurse on the ADDRESS of the left-child pointer so that
        // the recursive call can rewrite it if needed
        bst_delete(&amp;(*p)-&gt;left, x);

    } else if (x &gt; (*p)-&gt;value) {
        // x must be in the RIGHT subtree (BST property)
        bst_delete(&amp;(*p)-&gt;right, x);

    } else {
        // 3) found the node to delete: (*p)-&gt;value == x

        // --- CASE 1 + CASE 2 : the node has at most one child ---

        // case (a): no left child → replace *p with its right child
        // (this also handles the leaf case: then (*p)-&gt;right is NULL,
        //  so *p becomes NULL — exactly what removing a leaf means)
        if ((*p)-&gt;left == NULL) {
            struct node *q = *p;          // q = remember the doomed node
            *p = (*p)-&gt;right;             // parent slot now points to right child
            free(q);                       // delete the old node
            return;
        }

        // case (b): no right child → replace *p with its left child
        if ((*p)-&gt;right == NULL) {
            struct node *q = *p;          // q = remember the doomed node
            *p = (*p)-&gt;left;              // parent slot now points to left child
            free(q);                       // delete the old node
            return;
        }

        // --- CASE 3 : the node has TWO children ---

        // find the inorder successor: the minimum of the right subtree.
        // this node is guaranteed to have NO left child (leftmost) and
        // therefore has at most one child → easy to remove later.
        struct node *tmp = bst_min((*p)-&gt;right);

        // copy ONLY the VALUE of the successor into the current node.
        // the structural node we want to remove is now &quot;tmp&quot; but its
        // value already lives here, so the BST still looks correct.
        (*p)-&gt;value = tmp-&gt;value;

        // delete the successor node from the right subtree.
        // since the successor has at most one child, this recursive
        // call hits CASE 1 or CASE 2 and terminates quickly.
        bst_delete(&amp;(*p)-&gt;right, tmp-&gt;value);
    }
}</code></pre>
<b>Complexity:</b> <code>O(h)</code>."	Info2::Binary_Trees_BST
"<b>BST</b> — Code: write a function that determines the height of a BST. What is the asymptotic complexity?"	"<b>Convention used here:</b>
<ul>
<li>height of an empty tree = <code>-1</code></li>
<li>height of a single-node tree = <code>0</code></li>
</ul>
<pre><code>struct node {
    int value;
    struct node *left;
    struct node *right;
};

int max(int a, int b) {
    if (a &gt; b) return a;
    return b;
}

int height(struct node *root) {
    // base case: empty subtree
    if (root == NULL) return -1;

    // recurse into both subtrees
    int lh = height(root-&gt;left);
    int rh = height(root-&gt;right);

    // height of this node = 1 + height of taller child
    return 1 + max(lh, rh);
}</code></pre>
<b>Complexity:</b>
<ul>
<li><b>Time:</b> <b>O(n)</b> — every node is visited exactly once and does O(1) work.</li>
<li><b>Space:</b> <b>O(h)</b> for the recursion stack (= O(log n) average, O(n) worst).</li>
</ul>
<b>Note on conventions:</b> some courses define height of an empty tree as 0 (then leaf = 1). Match the convention your prof uses."	Info2::Binary_Trees_BST
"<b>Search strategies</b> — Explain <i>Breadth-First Search (BFS)</i> vs <i>Depth-First Search (DFS)</i>. What problems is each used for?"	"<b>BFS — Breadth-First Search:</b>
<ul>
<li>Explores all nodes at the current depth <i>before</i> going deeper.</li>
<li>Visits the tree / graph <b>level by level</b>.</li>
<li>Uses a <b>queue (FIFO)</b>.</li>
</ul>
<b>BFS is used for:</b>
<ul>
<li>Shortest path in an <b>unweighted</b> graph (counted in edges).</li>
<li>Finding the closest match to a source.</li>
<li>Level-order tree traversal.</li>
<li>Connected components in undirected graphs.</li>
</ul>
<b>DFS — Depth-First Search:</b>
<ul>
<li>Goes as <b>deep</b> as possible along each branch, then backtracks.</li>
<li>Uses a <b>stack (LIFO)</b> — explicitly, or implicitly via recursion.</li>
<li>The three tree traversals (inorder, preorder, postorder) are all DFS.</li>
</ul>
<b>DFS is used for:</b>
<ul>
<li>Detecting <b>cycles</b> in a graph.</li>
<li><b>Topological sort</b> of a DAG.</li>
<li>Path existence between two nodes.</li>
<li>All standard tree traversals (inorder / preorder / postorder).</li>
<li>Maze / puzzle solving by exploring full paths.</li>
<li>Finding strongly connected components (Tarjan, Kosaraju).</li>
</ul>
<b>Quick comparison:</b>
<table style=&quot;border-collapse: collapse;&quot;>
<tr><th style=&quot;text-align:left; padding-right:18px;&quot;></th><th style=&quot;text-align:left; padding-right:18px;&quot;>BFS</th><th style=&quot;text-align:left;&quot;>DFS</th></tr>
<tr><td>structure</td><td>queue</td><td>stack / recursion</td></tr>
<tr><td>order</td><td>level by level</td><td>deep first</td></tr>
<tr><td>shortest path (unweighted)</td><td>YES</td><td>no</td></tr>
<tr><td>memory</td><td>O(width)</td><td>O(height)</td></tr>
</table>"	Info2::Binary_Trees_BST
"<b>AVL</b> — What is the balancing rule behind the AVL tree?"	"For <b>every</b> node <code>v</code> in the tree, the <b>balance factor</b> satisfies:
<ul>
<li><code>bf(v) = height(left subtree) − height(right subtree)</code></li>
<li><code>|bf(v)| ≤ 1</code></li>
</ul>
i.e. the heights of the left and right subtree differ by at most <b>1</b>. The condition must hold <b>recursively</b> for every node.
<br><br>
<b>Height convention used here:</b>
<ul>
<li><code>height(NULL) = −1</code></li>
<li><code>height(leaf) = 0</code></li>
</ul>
With this convention, the balance factor of a leaf is <code>0</code> and the height is computed by <code>1 + max(height(left), height(right))</code>."	Info2::AVL_Trees
"<b>AVL</b> — Fill in the blanks. <i>AVL trees guarantee ___ complexity for all operations. The height of an AVL tree with n nodes is always ___.</i> What is so special about the height of an AVL tree?"	"<b>Guarantees:</b>
<ul>
<li>AVL trees guarantee <b>O(log n)</b> worst-case complexity for <b>search</b>, <b>insert</b>, and <b>delete</b>.</li>
<li>The height of an AVL tree with <code>n</code> nodes is always <b>Θ(log n)</b>.<br>
More precisely: <code>h ≤ 1.44 · log₂(n + 2)</code>.</li>
</ul>
<b>Why is this special?</b>
<ul>
<li>A plain BST can degenerate to height <code>O(n)</code> for bad insertion orders (e.g. inserting sorted input → linked list).</li>
<li>The AVL balancing property <b>forces</b> the height to stay logarithmic no matter what sequence of insertions/deletions is applied.</li>
<li>This is what makes search/insert/delete <b>guaranteed</b> <code>O(log n)</code> — not just on average.</li>
</ul>"	Info2::AVL_Trees
"<b>AVL</b> — What is the structure of an AVL tree node in C? What is the difference compared to a BST node?"	"An AVL node is a BST node with <b>one extra field</b>: the cached height (or, equivalently, the balance factor).
<pre><code>struct node {
    int value;
    int height;            // &lt;-- new: cached height for fast rebalancing
    struct node *left;
    struct node *right;
};

// returns -1 for NULL so the recursive height formula works for leaves
int height(struct node *p) {
    if (p == NULL) return -1;
    return p-&gt;height;
}

int max(int a, int b) {
    if (a &gt; b) return a;
    return b;
}</code></pre>
<b>Difference vs BST:</b> only the extra <code>height</code> field. Some implementations store the <i>balance factor</i> directly instead — both approaches work; storing the height is slightly more flexible."	Info2::AVL_Trees
"<b>AVL</b> — When do you perform a right rotation? Code the algorithm for right rotation."	"<b>When?</b> When a node's <b>LEFT subtree is too tall</b> and the left child is itself <i>left-heavy or balanced</i> (LL case). A right rotation pulls the left child upward.
<br><br>
<b>Picture:</b>
<pre><code>     y                    x
    / \                  / \
   x   T3       →       T1  y
  / \                       / \
 T1  T2                    T2  T3</code></pre>
<b>C code:</b>
<pre><code>struct node *right_rotate(struct node *y) {
    struct node *x  = y-&gt;left;
    struct node *T2 = x-&gt;right;

    // perform rotation: x moves up, y moves down to x's right
    x-&gt;right = y;
    y-&gt;left  = T2;

    // update heights — lowest node FIRST
    y-&gt;height = 1 + max(height(y-&gt;left), height(y-&gt;right));
    x-&gt;height = 1 + max(height(x-&gt;left), height(x-&gt;right));

    return x;   // x is the new root of this subtree
}</code></pre>"	Info2::AVL_Trees
"<b>AVL</b> — When do you perform a left rotation? Code the algorithm for left rotation."	"<b>When?</b> When a node's <b>RIGHT subtree is too tall</b> and the right child is itself <i>right-heavy or balanced</i> (RR case). A left rotation pulls the right child upward.
<br><br>
<b>Picture:</b>
<pre><code>    x                        y
   / \                      / \
  T1  y           →        x   T3
     / \                  / \
    T2  T3               T1  T2</code></pre>
<b>C code:</b>
<pre><code>struct node *left_rotate(struct node *x) {
    struct node *y  = x-&gt;right;
    struct node *T2 = y-&gt;left;

    // perform rotation: y moves up, x moves down to y's left
    y-&gt;left  = x;
    x-&gt;right = T2;

    // update heights — lowest node FIRST
    x-&gt;height = 1 + max(height(x-&gt;left), height(x-&gt;right));
    y-&gt;height = 1 + max(height(y-&gt;left), height(y-&gt;right));

    return y;   // y is the new root of this subtree
}</code></pre>"	Info2::AVL_Trees
"<b>AVL</b> — When do you perform a left-right rotation? When a right-left rotation? Code both fully."	"<b>LR rotation — when?</b> When a node's <b>LEFT subtree is too tall</b> but the <b>left child is RIGHT-heavy</b>. A single right rotation is not enough — we first <i>left-rotate</i> the left child (which turns LR into LL), then <i>right-rotate</i> the node.
<br><br>
<b>LR picture:</b>
<pre><code>     z                  z                    y
    / \                / \                  / \
   x   T4             y   T4               x   z
  / \      →         / \         →        / \ / \
 T1  y              x   T3              T1 T2 T3 T4
    / \            / \
   T2  T3         T1  T2</code></pre>
<b>LR code:</b>
<pre><code>struct node *left_right_rotate(struct node *p) {
    // step 1: left-rotate the left child → now LL shape
    p-&gt;left = left_rotate(p-&gt;left);

    // step 2: right-rotate p
    return right_rotate(p);
}</code></pre>
<br>
<b>RL rotation — when?</b> When a node's <b>RIGHT subtree is too tall</b> but the <b>right child is LEFT-heavy</b>. Mirror of LR: first <i>right-rotate</i> the right child (which turns RL into RR), then <i>left-rotate</i> the node.
<br><br>
<b>RL picture:</b>
<pre><code>   z                    z                       y
  / \                  / \                     / \
 T1  x                T1  y                   z   x
    / \      →           / \       →        / \ / \
   y   T4               T2  x              T1 T2 T3 T4
  / \                       / \
 T2  T3                    T3  T4</code></pre>
<b>RL code:</b>
<pre><code>struct node *right_left_rotate(struct node *p) {
    // step 1: right-rotate the right child → now RR shape
    p-&gt;right = right_rotate(p-&gt;right);

    // step 2: left-rotate p
    return left_rotate(p);
}</code></pre>
<b>In practice</b> these are usually inlined inside <code>AVLrebalance</code> rather than written as standalone functions."	Info2::AVL_Trees
"<b>AVL</b> — After an insertion, there are 4 cases that can violate the AVL property. Name them and the fix for each."	"<table style=&quot;border-collapse: collapse;&quot;>
<tr>
  <th style=&quot;text-align:left; padding-right:18px;&quot;>Case</th>
  <th style=&quot;text-align:left; padding-right:18px;&quot;>Trigger</th>
  <th style=&quot;text-align:left; padding-right:18px;&quot;>bf(v)</th>
  <th style=&quot;text-align:left; padding-right:18px;&quot;>bf(child)</th>
  <th style=&quot;text-align:left;&quot;>Fix</th>
</tr>
<tr><td><b>LL</b></td><td>insert into <b>L</b>eft subtree of <b>L</b>eft child</td><td>&gt; 1</td><td>≥ 0</td><td><code>right_rotate(v)</code></td></tr>
<tr><td><b>LR</b></td><td>insert into <b>R</b>ight subtree of <b>L</b>eft child</td><td>&gt; 1</td><td>&lt; 0</td><td><code>left_rotate(v-&gt;left)</code> then <code>right_rotate(v)</code></td></tr>
<tr><td><b>RR</b></td><td>insert into <b>R</b>ight subtree of <b>R</b>ight child</td><td>&lt; −1</td><td>≤ 0</td><td><code>left_rotate(v)</code></td></tr>
<tr><td><b>RL</b></td><td>insert into <b>L</b>eft subtree of <b>R</b>ight child</td><td>&lt; −1</td><td>&gt; 0</td><td><code>right_rotate(v-&gt;right)</code> then <code>left_rotate(v)</code></td></tr>
</table>
<br>
<b>Mnemonic:</b>
<ul>
<li><b>LL</b> &amp; <b>RR</b> → ONE rotation (single).</li>
<li><b>LR</b> &amp; <b>RL</b> → TWO rotations (double rotation).</li>
<li>The first letter of the case tells you which side is too tall; the second letter tells you whether the child is also leaning the same way (single) or the opposite way (double).</li>
</ul>"	Info2::AVL_Trees
"<b>AVL</b> — What are the 5 steps for inserting an element in an AVL tree?"	"<ol>
<li><b>Insert like in a normal BST</b> — find the right slot and attach the new node as a leaf.</li>
<li><b>Move back up the recursion path</b> — every recursive call now returns to an ancestor of the inserted node.</li>
<li><b>Update heights</b> — recompute the height of each ancestor:<br>
<code>p-&gt;height = 1 + max(height(p-&gt;left), height(p-&gt;right));</code></li>
<li><b>Compute the balance factor</b> — <code>bf = height(p-&gt;left) − height(p-&gt;right);</code></li>
<li><b>Fix imbalances with rotations</b> — if <code>|bf| &gt; 1</code>, apply the matching rotation:<br>
LL → right-rotate, RR → left-rotate, LR → left + right, RL → right + left.</li>
</ol>
<b>Important:</b> after an insertion, <b>at most ONE rotation (single or double)</b> is needed to fix the entire tree."	Info2::AVL_Trees
"<b>AVL</b> — There are 3 cases of deletion in an AVL tree. Name them. How does deletion differ from insertion regarding rebalancing?"	"The <b>3 deletion cases</b> are the same as for a plain BST:
<ol>
<li><b>Node is a leaf</b> → simply remove it.</li>
<li><b>Node has exactly one child</b> → replace it with its child.</li>
<li><b>Node has two children</b> → replace its <i>value</i> with the value of its inorder successor (the minimum of the right subtree), then delete that successor in the right subtree (which falls into case 1 or 2).</li>
</ol>
<b>Difference vs insertion regarding rebalancing:</b>
<ul>
<li><b>After insertion:</b> at most ONE rebalancing along the path back to the root is enough.</li>
<li><b>After deletion:</b> up to <b>O(log n)</b> rebalancings — the height can shrink at the deleted spot, and that shrink can propagate upward all the way to the root, forcing many ancestors to be rebalanced.</li>
</ul>
That's why the AVL deletion algorithm <i>always</i> rebalances every ancestor on the way back up, not just the first imbalanced one."	Info2::AVL_Trees
"<b>AVL</b> — Code: one-liner to update the height of a node for rebalancing."	"<pre><code>p-&gt;height = 1 + max(height(p-&gt;left), height(p-&gt;right));</code></pre>
With the helper:
<pre><code>int height(struct node *p) {
    if (p == NULL) return -1;
    return p-&gt;height;
}</code></pre>
<b>Why it works:</b> a leaf has <code>height(NULL) = −1</code> for both children, so its height becomes <code>1 + max(−1, −1) = 0</code>. Every internal node is 1 higher than its taller child."	Info2::AVL_Trees
"<b>AVL</b> — Write the full rebalancing pseudocode <code>AVLrebalance(p)</code>. It must first update the height of <code>p</code>, contain all 4 cases (<b>LL, RR, LR, RL</b>), and return <code>p</code> (or the new subtree root after rotation)."	"<pre><code>struct node *AVLrebalance(struct node *p) {
    if (p == NULL) return NULL;

    // 1) update height of p
    p-&gt;height = 1 + max(height(p-&gt;left), height(p-&gt;right));

    // 2) compute balance factor of p
    int bf = height(p-&gt;left) - height(p-&gt;right);

    // 3) LEFT side too heavy  (bf &gt; 1)
    if (bf &gt; 1) {
        // LR case: left child is right-heavy
        // → first left-rotate the left child (turns LR into LL)
        if (height(p-&gt;left-&gt;left) &lt; height(p-&gt;left-&gt;right)) {
            p-&gt;left = left_rotate(p-&gt;left);
        }
        // LL case (or after LR conversion above): right-rotate p
        return right_rotate(p);
    }

    // 4) RIGHT side too heavy (bf &lt; -1)
    if (bf &lt; -1) {
        // RL case: right child is left-heavy
        // → first right-rotate the right child (turns RL into RR)
        if (height(p-&gt;right-&gt;right) &lt; height(p-&gt;right-&gt;left)) {
            p-&gt;right = right_rotate(p-&gt;right);
        }
        // RR case (or after RL conversion above): left-rotate p
        return left_rotate(p);
    }

    // 5) already balanced → nothing to do
    return p;
}</code></pre>
<b>Complexity:</b> <code>O(1)</code> per call (a constant number of pointer rewires + height updates)."	Info2::AVL_Trees
"<b>AVL</b> — How do insertion and deletion work conceptually in an AVL tree? Give pseudocode for both."	"<b>AVL Insertion — conceptual:</b>
<ol>
<li>Insert like in a normal BST.</li>
<li>On the way back up the recursion, update heights and call <code>AVLrebalance</code> on every ancestor.</li>
<li><i>At most ONE</i> rotation needed in total.</li>
</ol>
<pre><code>struct node *AVLInsert(struct node *root, int x) {
    // 1) standard BST insertion (no duplicates)
    if (root == NULL) {
        struct node *p = malloc(sizeof(struct node));
        p-&gt;value  = x;
        p-&gt;height = 0;          // fresh leaf has height 0
        p-&gt;left   = NULL;
        p-&gt;right  = NULL;
        return p;
    }
    if (x &lt; root-&gt;value) {
        root-&gt;left  = AVLInsert(root-&gt;left,  x);
    } else if (x &gt; root-&gt;value) {
        root-&gt;right = AVLInsert(root-&gt;right, x);
    } else {
        return root;             // duplicate → ignore
    }

    // 2) rebalance on the way up
    return AVLrebalance(root);
}</code></pre>

<b>AVL Deletion — conceptual:</b>
<ol>
<li>Delete like in a normal BST (the 3 cases).</li>
<li>On the way back up, update heights and call <code>AVLrebalance</code> on every ancestor.</li>
<li><i>Up to O(log n)</i> rotations possible because the shrink can propagate.</li>
</ol>
<pre><code>// deletes the node with value x from the AVL tree whose root is *p.
// p is a pointer-to-pointer so we can OVERWRITE *p in place; the
// recursion threads addresses through &amp;(*p)-&gt;left / &amp;(*p)-&gt;right.
// usage at the top level:  AVLDelete(&amp;root, x);
void AVLDelete(struct node **p, int x) {

    // 1) base case: x is not in the tree → nothing to do
    if (*p == NULL) return;

    // 2) navigate / delete (no early return — we want to fall through
    //    to the rebalance step at the bottom)
    if (x &lt; (*p)-&gt;value) {
        AVLDelete(&amp;(*p)-&gt;left, x);

    } else if (x &gt; (*p)-&gt;value) {
        AVLDelete(&amp;(*p)-&gt;right, x);

    } else {
        // found the node to delete

        // case 1 + 2: at most one child
        if ((*p)-&gt;left == NULL) {
            struct node *q = *p;          // remember doomed node
            *p = (*p)-&gt;right;             // parent slot → right child
            free(q);
        } else if ((*p)-&gt;right == NULL) {
            struct node *q = *p;
            *p = (*p)-&gt;left;              // parent slot → left child
            free(q);
        } else {
            // case 3: two children — copy successor value, then delete it
            struct node *tmp = bst_min((*p)-&gt;right);
            (*p)-&gt;value = tmp-&gt;value;
            AVLDelete(&amp;(*p)-&gt;right, tmp-&gt;value);
        }
    }

    // 3) rebalance on the way up (only if there is still a node here).
    //    AVLrebalance updates the height and applies LL/LR/RR/RL fixes.
    if (*p != NULL) {
        *p = AVLrebalance(*p);
    }
}</code></pre>
<b>Total time complexity:</b> <code>O(log n)</code> for both — <code>O(1)</code> rebalance work per level × <code>O(log n)</code> levels."	Info2::AVL_Trees
"<b>HT</b> — When do you use a hash table? When is it fastest? What is the time complexity of search, insert and delete?"	"<b>When to use:</b>
<ul>
<li>You need <b>fast lookup / insert / delete by key</b> and you do <b>not</b> need keys in sorted order.</li>
<li>Typical uses: dictionaries, sets, caches, symbol tables, frequency counting.</li>
</ul>
<b>When is it fastest?</b>
<ul>
<li>When the hash function distributes keys <b>uniformly</b> (no clustering) and the <b>load factor α</b> is kept low (e.g. &lt; 0.7 for open addressing).</li>
<li>Then all three core operations run in <b>expected O(1)</b>.</li>
</ul>
<b>Time complexity:</b>
<table style=&quot;border-collapse: collapse;&quot;>
<tr><th style=&quot;text-align:left; padding-right:18px;&quot;>Op</th><th style=&quot;text-align:left; padding-right:18px;&quot;>Average</th><th style=&quot;text-align:left;&quot;>Worst case</th></tr>
<tr><td>search</td><td><b>O(1)</b></td><td><b>O(n)</b></td></tr>
<tr><td>insert</td><td><b>O(1)</b></td><td><b>O(n)</b></td></tr>
<tr><td>delete</td><td><b>O(1)</b></td><td><b>O(n)</b></td></tr>
</table>
<br>
<b>Worst case</b> happens when all keys collide (every key hashes to the same slot / bucket) — then operations degrade to a linear scan."	Info2::Hash_Tables
"<b>HT</b> — Code: write the struct of a hash table in C. Add a comment that <code>int status</code> is either OCC, EMP, or DEL."	"<pre><code>#define EMP 0   // empty — slot has never been used
#define OCC 1   // occupied — slot holds a valid (key, value)
#define DEL 2   // deleted — tombstone; search must keep probing past it

struct slot {
    int key;      // the actual value we want to store (e.g. 18).
                  // NOT the hash — the hash is computed FROM this key.
    int value;    // optional payload attached to the key (e.g. a count
                  // or a related number). Omit this field for a set-only HT.
    int status;   // either OCC, EMP, or DEL
};

struct hash_table {
    struct slot *table;   // pointer to a dynamically allocated array of m slots
                          // (we use a pointer because the size m is decided
                          //  at runtime, not at compile time)
    int m;                // size of the table (number of slots)
};</code></pre>
<b>Note — simpler &quot;set-only&quot; variant from class:</b>
<pre><code>int table[SIZE];   // each slot just stores an integer key,
                   // no value, no status (uses e.g. -1 for empty)</code></pre>
This is more compact but harder to extend (no payload, no DEL state).
<br><br>
<b>Why we need 3 statuses (not just OCC / EMP):</b> with open addressing we cannot simply mark a deleted slot as EMP — that would break the probe sequence of other keys (see the deletion card). The <b>DEL</b> tombstone preserves the probe path while marking the slot as reusable for inserts."	Info2::Hash_Tables
"<b>HT</b> — Two methods of hashing: <b>division</b> and <b>multiplication</b>. State both formulas and give a good choice for <code>m</code> (division) and <code>A</code> (multiplication)."	"<b>1) Division method</b>
<pre><code>h(k) = k mod m</code></pre>
<ul>
<li>Fast and easy.</li>
<li><b>Good choice for m:</b> a <b>prime</b> that is <b>not close to a power of 2</b> (or a power of 10). Typical examples: <b>7, 11, 13, 17, 31</b>, 97, 1009, …</li>
<li>Why? If <code>m = 2^p</code>, then <code>h(k)</code> = the last <code>p</code> bits of <code>k</code> → the high bits are ignored → terrible distribution.</li>
</ul>
<b>2) Multiplication method</b>
<pre><code>h(k) = floor( m · ( k · A  mod  1 ) )</code></pre>
where <code>0 &lt; A &lt; 1</code>. Take the fractional part of <code>k · A</code>, multiply by <code>m</code>, then floor.
<ul>
<li><b>Good choice for A:</b> Knuth's value <code>A ≈ (√5 − 1) / 2 ≈ 0.6180339887</code> (the golden-ratio conjugate).</li>
<li><b>Value of m</b> is much less critical — often a power of 2 for speed.</li>
</ul>"	Info2::Hash_Tables
"<b>Matrix Multiplication DP</b> — How can we find the best parenthesis split for matrix multiplication with a helper matrix? What values are calculated there? Code the DP solution for the minimum multiplication cost in C, iteratively and recursively. Explain the difference."	"<b>Problem:</b><br>
Given matrices
<pre><code>A1, A2, ..., An</code></pre>
we want to choose the parentheses so that the number of scalar multiplications is minimal.
<br><br>
If matrix <code>Ai</code> has size <code>d[i-1] × d[i]</code>, then multiplying
<pre><code>(Ai ... Ak) · (A(k+1) ... Aj)</code></pre>
costs:
<pre><code>M[i][k] + M[k+1][j] + d[i-1] * d[k] * d[j]</code></pre>
<b>Helper matrix:</b>
<ul>
<li><code>M[i][j]</code> stores the <b>minimum cost</b> to multiply matrices <code>Ai ... Aj</code>.</li>
<li><code>split[i][j]</code> can store the best split position <code>k</code>, so we know where to put the parentheses.</li>
<li>Base case: <code>M[i][i] = 0</code>, because one matrix alone needs no multiplication.</li>
</ul>
<b>Variable meanings:</b>
<ul>
<li><code>d[]</code>: dimension array. Matrix <code>Ai</code> has size <code>d[i-1] × d[i]</code>.</li>
<li><code>n</code>: number of matrices, so <code>d</code> has length <code>n + 1</code>.</li>
<li><code>i, j</code>: first and last matrix index of the current subchain.</li>
<li><code>k</code>: split point; it separates <code>Ai..Ak</code> from <code>A(k+1)..Aj</code>.</li>
<li><code>q</code>: cost of one candidate split.</li>
</ul>
<b>Iterative bottom-up version:</b>
<pre><code>#include &lt;limits.h&gt;

int matrix_chain_iterative(int d[], int n) {
    // M[i][j] = minimum cost to multiply Ai ... Aj
    int M[n + 1][n + 1];

    // split[i][j] = best k where Ai ... Aj should be split
    int split[n + 1][n + 1];

    // one matrix alone costs 0 multiplications
    for (int i = 1; i &lt;= n; i++) {
        M[i][i] = 0;
    }

    // len is the chain length minus 1, i.e. distance j - i
    // we solve shorter chains before longer chains
    for (int len = 1; len &lt;= n - 1; len++) {
        for (int i = 1; i &lt;= n - len; i++) {
            // current subproblem: multiply Ai ... Aj
            int j = i + len;
            M[i][j] = INT_MAX;

            // try every possible final split position k
            for (int k = i; k &lt;= j - 1; k++) {
                // cost = left subchain + right subchain + final multiplication
                int q = M[i][k] + M[k + 1][j]
                      + d[i - 1] * d[k] * d[j];

                // keep the cheapest split found so far
                if (q &lt; M[i][j]) {
                    M[i][j] = q;
                    split[i][j] = k;
                }
            }
        }
    }

    return M[1][n];
}</code></pre>
<b>Recursive memoized version:</b>
<pre><code>#include &lt;limits.h&gt;

int matrix_memo_rec(int d[], int i, int j, int n, int M[n + 1][n + 1]) {
    // base case: one matrix needs no multiplication
    if (i == j) {
        return 0;
    }

    // if this subproblem was already solved, reuse it
    if (M[i][j] &lt; INT_MAX) {
        return M[i][j];
    }

    // otherwise try all possible split positions k
    for (int k = i; k &lt;= j - 1; k++) {
        // recursively solve left and right side of the split
        int q = d[i - 1] * d[k] * d[j]
              + matrix_memo_rec(d, i, k, n, M)
              + matrix_memo_rec(d, k + 1, j, n, M);

        // remember the best cost for Ai ... Aj
        if (q &lt; M[i][j]) {
            M[i][j] = q;
        }
    }

    return M[i][j];
}

int matrix_chain_recursive(int d[], int n) {
    int M[n + 1][n + 1];

    // INT_MAX means &quot;not computed yet&quot;
    for (int i = 1; i &lt;= n; i++) {
        for (int j = 1; j &lt;= n; j++) {
            M[i][j] = INT_MAX;
        }
    }

    // solve the full chain A1 ... An
    return matrix_memo_rec(d, 1, n, n, M);
}</code></pre>
<b>Difference:</b>
<ul>
<li><b>Iterative DP:</b> fills the table by increasing subproblem length, so every needed value is already available.</li>
<li><b>Recursive memoization:</b> starts with the full problem and recursively asks for smaller subproblems; each solved value is stored in <code>M</code> so it is not recomputed.</li>
<li>Both compute the same recurrence and have <code>O(n³)</code> time and <code>O(n²)</code> space.</li>
</ul>"	Info2::Dynamic_Programming
"<b>Longest Common Subsequence</b> — What does the LCS of two strings measure? Explain the two cases when comparing the strings. Code the dynamic programming solution in C that returns the length <code>c[m][n]</code>."	"<b>Meaning:</b><br>
The <b>longest common subsequence</b> measures the length of the longest sequence of characters that appears in both strings in the same order.
<br><br>
Important: a subsequence does <b>not</b> have to be contiguous. A substring would have to be contiguous.
<br><br>
Example:
<pre><code>X = ABCBDAB
Y = BDCABA

BCBA is a common subsequence of length 4.</code></pre>
<b>DP table:</b><br>
Let <code>c[i][j]</code> be the LCS length of the prefixes:
<pre><code>X[0 ... i-1] and Y[0 ... j-1]</code></pre>
So the final answer is <code>c[m][n]</code>.
<br><br>
<b>Variable meanings:</b>
<ul>
<li><code>X</code>, <code>Y</code>: the two input strings.</li>
<li><code>m</code>, <code>n</code>: lengths of <code>X</code> and <code>Y</code>.</li>
<li><code>i</code>, <code>j</code>: prefix lengths currently compared.</li>
<li><code>c[i][j]</code>: LCS length of <code>X[0..i-1]</code> and <code>Y[0..j-1]</code>.</li>
</ul>
<b>Two cases:</b>
<ol>
<li>If the current characters are equal, they can extend the LCS:
<pre><code>if X[i-1] == Y[j-1]:
    c[i][j] = c[i-1][j-1] + 1</code></pre></li>
<li>If they are different, ignore one of the two current characters and take the better result:
<pre><code>else:
    c[i][j] = max(c[i-1][j], c[i][j-1])</code></pre></li>
</ol>
<b>C code:</b>
<pre><code>int lcs_length(char X[], int m, char Y[], int n) {
    // c[i][j] stores the answer for prefixes of length i and j
    int c[m + 1][n + 1];

    // empty Y: LCS length is 0
    for (int i = 0; i &lt;= m; i++) {
        c[i][0] = 0;
    }

    // empty X: LCS length is 0
    for (int j = 0; j &lt;= n; j++) {
        c[0][j] = 0;
    }

    // fill the table from small prefixes to large prefixes
    for (int i = 1; i &lt;= m; i++) {
        for (int j = 1; j &lt;= n; j++) {
            // X[i-1] and Y[j-1] are the current last characters
            if (X[i - 1] == Y[j - 1]) {
                // same character: extend the LCS of the smaller prefixes
                c[i][j] = c[i - 1][j - 1] + 1;
            } else if (c[i - 1][j] &gt;= c[i][j - 1]) {
                // different characters: ignore X[i-1]
                c[i][j] = c[i - 1][j];
            } else {
                // different characters: ignore Y[j-1]
                c[i][j] = c[i][j - 1];
            }
        }
    }

    // answer for the full strings X and Y
    return c[m][n];
}</code></pre>
<b>Complexity:</b> <code>O(mn)</code> time and <code>O(mn)</code> space."	Info2::Dynamic_Programming
"<b>Coin Change DP</b> — Given an amount <code>A</code> and <code>m</code> denominations, how do we change <code>A</code> with as few coins as possible? Explain the three possible cases and give a dynamic programming solution in C."	"<b>Problem:</b><br>
Given an amount <code>A</code> and coin denominations
<pre><code>coin[0], coin[1], ..., coin[m-1]</code></pre>
find the minimum number of coins needed to make exactly amount <code>A</code>.
<br><br>
<b>Three possible cases:</b>
<ol>
<li><b>No solution:</b> The amount cannot be built from the given coins. Example: <code>A = 3</code>, coins <code>{2}</code>.</li>
<li><b>Greedy is optimal:</b> Always taking the largest possible coin gives the best answer. Many real currencies are designed like this.</li>
<li><b>Need DP:</b> Greedy can fail. Example: <code>A = 12</code>, coins <code>{10, 6, 1}</code>. Greedy gives <code>10 + 1 + 1</code> = 3 coins, but the optimum is <code>6 + 6</code> = 2 coins.</li>
</ol>
<b>DP idea:</b><br>
Let <code>dp[a]</code> be the minimum number of coins needed for amount <code>a</code>.
<pre><code>dp[0] = 0
dp[a] = 1 + min(dp[a - coin[i]]) over all usable coins coin[i] &lt;= a</code></pre>
If <code>dp[A]</code> is still infinity at the end, there is no solution.
<br><br>
<b>Variable meanings:</b>
<ul>
<li><code>coins[]</code>: array of available coin denominations.</li>
<li><code>m</code>: number of different denominations.</li>
<li><code>A</code>: target amount.</li>
<li><code>a</code>: current amount for which we compute the best answer.</li>
<li><code>dp[a]</code>: minimum number of coins needed to form amount <code>a</code>.</li>
<li><code>candidate</code>: number of coins if we use the current coin as the last coin.</li>
</ul>
<b>C code:</b>
<pre><code>#include &lt;limits.h&gt;

int min_coins(int coins[], int m, int A) {
    // dp[a] = minimum number of coins needed to make amount a
    int dp[A + 1];

    // amount 0 needs 0 coins
    dp[0] = 0;

    // all other amounts start as impossible
    for (int a = 1; a &lt;= A; a++) {
        dp[a] = INT_MAX;
    }

    // compute the best answer for every amount from 1 to A
    for (int a = 1; a &lt;= A; a++) {
        // try every coin as the last coin used for amount a
        for (int i = 0; i &lt; m; i++) {
            int coin = coins[i];

            // coin must fit, and the remaining amount must be solvable
            if (coin &lt;= a &amp;&amp; dp[a - coin] != INT_MAX) {
                int candidate = dp[a - coin] + 1;

                // keep the solution with the fewest coins
                if (candidate &lt; dp[a]) {
                    dp[a] = candidate;
                }
            }
        }
    }

    // still infinity means no combination of coins makes A
    if (dp[A] == INT_MAX) {
        return -1;   // no solution
    }

    // minimum number of coins for the target amount
    return dp[A];
}</code></pre>
<b>Complexity:</b> <code>O(A · m)</code> time and <code>O(A)</code> space."	Info2::Dynamic_Programming
"<b>Exam 25 DP — Nearest Zero in a Matrix</b> — Given an <code>m × n</code> integer matrix <code>M</code>, compute for every cell the Manhattan distance to the nearest zero. Explain and code <code>nD</code> for zeros in the north, and <code>D</code> for zeros in all directions. The solution must run in <code>O(mn)</code>."	"<b>Problem:</b><br>
For each position <code>[i, j]</code>, we want the distance to the closest cell <code>[p, q]</code> with <code>M[p][q] == 0</code>. The distance is:
<pre><code>|p - i| + |q - j|</code></pre>
We store the answers in a matrix <code>S</code> with the same dimensions as <code>M</code>.
<br><br>
<b>Idea for <code>nD</code>:</b><br>
If we only look <b>north</b> in the same column, then every cell depends only on the cell directly above it:
<ul>
<li>If <code>M[i][j] == 0</code>, the distance is <code>0</code>.</li>
<li>If we are in the first row and the cell is not zero, there is no zero above it, so we store infinity.</li>
<li>Otherwise the nearest northern zero is one step farther than the nearest northern zero from <code>[i-1, j]</code>.</li>
</ul>
<b>Variable meanings:</b>
<ul>
<li><code>M</code>: input matrix.</li>
<li><code>S</code>: solution matrix; <code>S[i][j]</code> stores the best distance found for cell <code>[i,j]</code>.</li>
<li><code>m</code>, <code>n</code>: number of rows and columns.</li>
<li><code>i</code>, <code>j</code>: current row and column.</li>
<li><code>INF</code>: a value larger than any possible Manhattan distance in the matrix.</li>
</ul>
<b>C code for north only:</b>
<pre><code>#define INF (m + n)

void nD(int m, int n, int M[m][n], int S[m][n]) {
    // Scan rows from top to bottom.
    // This direction is important because S[i-1][j] must already be known.
    for (int i = 0; i &lt; m; i++) {
        // Scan every column in the current row.
        for (int j = 0; j &lt; n; j++) {
            // Case 1: the current cell itself is zero.
            if (M[i][j] == 0) {
                // Distance from a zero cell to itself is 0.
                S[i][j] = 0;
            } else if (i == 0) {
                // Case 2: first row and not zero.
                // There cannot be any zero north of this cell.
                S[i][j] = INF;
            } else {
                // Case 3: use the cell directly above.
                // If the nearest northern zero above [i-1,j] has distance d,
                // then from [i,j] it is one step farther.
                S[i][j] = S[i - 1][j] + 1;
            }
        }
    }
}</code></pre>
<b>Idea for <code>D</code> in all directions:</b><br>
A shortest path to a zero can approach a cell from north, south, west, or east. We therefore do several linear passes over the matrix and relax the value from neighboring cells:
<ol>
<li>Top to bottom: distances from the north.</li>
<li>Bottom to top: distances from the south.</li>
<li>Left to right: distances from the west.</li>
<li>Right to left: distances from the east.</li>
</ol>
Each pass uses the rule:
<pre><code>S[current] = min(S[current], S[neighbor] + 1)</code></pre>
<b>C code for all directions:</b>
<pre><code>int min(int a, int b) {
    // Return the smaller of two candidate distances.
    return a &lt; b ? a : b;
}

void D(int m, int n, int M[m][n], int S[m][n]) {
    // maximum useful distance is at most m + n - 2,
    // so m + n works as infinity
    int INF = m + n;

    // ----- pass 1: top to bottom -----
    // Initialize S with distances to zeros in the north direction.
    for (int i = 0; i &lt; m; i++) {
        for (int j = 0; j &lt; n; j++) {
            // A zero cell is always distance 0 from the nearest zero.
            if (M[i][j] == 0) {
                S[i][j] = 0;
            } else if (i == 0 || S[i - 1][j] == INF) {
                // If we are in row 0, there is no northern neighbor.
                // Or if the cell above has no northern zero, neither do we.
                S[i][j] = INF;
            } else {
                // Otherwise, the best northern zero is one step above us.
                S[i][j] = S[i - 1][j] + 1;
            }
        }
    }

    // ----- pass 2: bottom to top -----
    // Improve S using possible zeros south of each cell.
    for (int i = m - 2; i &gt;= 0; i--) {
        for (int j = 0; j &lt; n; j++) {
            // If the cell below has distance d, then this cell can reach
            // the same zero with distance d + 1.
            S[i][j] = min(S[i][j], S[i + 1][j] + 1);
        }
    }

    // ----- pass 3: left to right -----
    // Improve S using possible zeros west of each cell.
    for (int j = 1; j &lt; n; j++) {
        for (int i = 0; i &lt; m; i++) {
            // If the left neighbor has distance d, current cell can reach
            // that zero with distance d + 1.
            S[i][j] = min(S[i][j], S[i][j - 1] + 1);
        }
    }

    // ----- pass 4: right to left -----
    // Improve S using possible zeros east of each cell.
    for (int j = n - 2; j &gt;= 0; j--) {
        for (int i = 0; i &lt; m; i++) {
            // If the right neighbor has distance d, current cell can reach
            // that zero with distance d + 1.
            S[i][j] = min(S[i][j], S[i][j + 1] + 1);
        }
    }
}</code></pre>
<b>Complexity:</b> four passes over the matrix, so <code>O(mn)</code> time and <code>O(1)</code> extra space besides the output matrix <code>S</code>."	Info2::Dynamic_Programming
"<b>Exam 24 DP — Maximum Product Subarray</b> — Given an array <code>A</code> with <code>n</code> integers, find the contiguous subarray with the largest product. Explain the DP idea and write <code>int psub(int A[], int n)</code> in C in <code>O(n)</code>."	"<b>Problem:</b><br>
Find indices <code>i..j</code> such that the product
<pre><code>A[i] * A[i+1] * ... * A[j]</code></pre>
is as large as possible.
<br><br>
<b>Key idea:</b><br>
For sums, it is enough to remember the best subarray ending at <code>i</code>. For products, negative numbers make this trickier:
<ul>
<li>A large positive product times a negative number becomes a large negative product.</li>
<li>A very small negative product times a negative number can become the new maximum.</li>
</ul>
So we keep two DP values:
<ul>
<li><code>max_end</code>: maximum product of a subarray ending at the current position.</li>
<li><code>min_end</code>: minimum product of a subarray ending at the current position.</li>
</ul>
For every <code>A[i]</code>, the best/worst product ending there is one of:
<pre><code>A[i]
max_end * A[i]
min_end * A[i]</code></pre>
<b>Variable meanings:</b>
<ul>
<li><code>A[]</code>: input array.</li>
<li><code>n</code>: number of elements in <code>A</code>.</li>
<li><code>x</code>: current value <code>A[i]</code>.</li>
<li><code>max_end</code>: maximum product of a contiguous subarray ending at the current index.</li>
<li><code>min_end</code>: minimum product of a contiguous subarray ending at the current index.</li>
<li><code>best</code>: best product found anywhere so far.</li>
</ul>
<b>C code:</b>
<pre><code>int max3(int a, int b, int c) {
    // First compare a and b.
    // m is the larger of those first two values.
    int m = a &gt; b ? a : b;

    // Now compare that temporary maximum with c.
    // The result is the maximum of all three values.
    return m &gt; c ? m : c;
}

int min3(int a, int b, int c) {
    // First compare a and b.
    // m is the smaller of those first two values.
    int m = a &lt; b ? a : b;

    // Now compare that temporary minimum with c.
    // The result is the minimum of all three values.
    return m &lt; c ? m : c;
}

int psub(int A[], int n) {
    // At index 0, the only subarray ending here is [A[0]].
    // Therefore the maximum product ending here is A[0].
    int max_end = A[0];

    // Same for the minimum product ending here.
    // We keep this because multiplying by a negative can turn min into max.
    int min_end = A[0];

    // best stores the best product over ALL subarrays seen so far.
    int best = A[0];

    // Process every later element once from left to right.
    for (int i = 1; i &lt; n; i++) {
        // x is the current array value A[i].
        // It may start a new subarray or extend a previous one.
        int x = A[i];

        // Save old values before overwriting max_end/min_end.
        // Both new states depend on BOTH previous states.
        int old_max = max_end;
        int old_min = min_end;

        // Compute the maximum product of a subarray ending at i.
        // Option 1: start a new subarray with x.
        // Option 2: extend previous max product by x.
        // Option 3: extend previous min product by x.
        max_end = max3(x, old_max * x, old_min * x);

        // Compute the minimum product ending at i.
        // This is needed because a negative x can turn a very negative
        // product into a very large positive product later.
        min_end = min3(x, old_max * x, old_min * x);

        // If the best subarray ending at i is better than the global best,
        // update the global answer.
        if (max_end &gt; best) {
            best = max_end;
        }
    }

    // best is the largest product over all contiguous subarrays.
    return best;
}</code></pre>
<b>Complexity:</b> <code>O(n)</code> time and <code>O(1)</code> extra space."	Info2::Dynamic_Programming
"<b>Exam 23 DP — Edit Distance</b> — Given strings <code>X[0..n-1]</code> and <code>Y[0..m-1]</code>, compute the minimum number of insertions, deletions, and substitutions needed to transform <code>X</code> into <code>Y</code>. Write <code>int ed(char X[], int n, char Y[], int m)</code>."	"<b>Problem:</b><br>
The edit distance is the minimum number of operations needed to change <code>X</code> into <code>Y</code>. Allowed operations:
<ul>
<li><b>Insert</b> a character into <code>X</code>.</li>
<li><b>Delete</b> a character from <code>X</code>.</li>
<li><b>Substitute</b> one character of <code>X</code> by another character.</li>
</ul>
<b>DP idea:</b><br>
Let <code>DP[i][j]</code> be the edit distance between the prefixes:
<pre><code>X[0..i-1] and Y[0..j-1]</code></pre>
Base cases:
<ul>
<li><code>DP[i][0] = i</code>: delete all <code>i</code> characters of <code>X</code>.</li>
<li><code>DP[0][j] = j</code>: insert all <code>j</code> characters of <code>Y</code>.</li>
</ul>
Transition:
<ul>
<li>If <code>X[i-1] == Y[j-1]</code>, no operation is needed for the last character.</li>
<li>Otherwise take the minimum of delete, insert, and substitute.</li>
</ul>
<b>Variable meanings:</b>
<ul>
<li><code>X</code>, <code>Y</code>: source and target string.</li>
<li><code>n</code>, <code>m</code>: lengths of <code>X</code> and <code>Y</code>.</li>
<li><code>i</code>, <code>j</code>: prefix lengths currently compared.</li>
<li><code>DP[i][j]</code>: edit distance between <code>X[0..i-1]</code> and <code>Y[0..j-1]</code>.</li>
<li><code>del</code>, <code>ins</code>, <code>sub</code>: costs for delete, insert, and substitute.</li>
</ul>
<b>C code:</b>
<pre><code>int min(int a, int b) {
    // Compare two candidate edit costs.
    // The smaller one is the better edit sequence.
    return a &lt; b ? a : b;
}

int ed(char X[], int n, char Y[], int m) {
    // DP[i][j] = edit distance between first i chars of X
    //            and first j chars of Y
    int DP[n + 1][m + 1];

    // Base case:
    // empty X prefix to empty Y prefix needs 0 operations.
    DP[0][0] = 0;

    // First column:
    // To transform X[0..i-1] into the empty string,
    // delete all i characters.
    for (int i = 1; i &lt;= n; i++) {
        // One more character in X means one more deletion.
        DP[i][0] = DP[i - 1][0] + 1;
    }

    // First row:
    // To transform the empty string into Y[0..j-1],
    // insert all j characters.
    for (int j = 1; j &lt;= m; j++) {
        // One more character in Y means one more insertion.
        DP[0][j] = DP[0][j - 1] + 1;
    }

    // Fill the table from small prefixes to large prefixes.
    for (int i = 1; i &lt;= n; i++) {
        for (int j = 1; j &lt;= m; j++) {
            // Compare the last characters of the current prefixes.
            if (X[i - 1] == Y[j - 1]) {
                // Last characters already match.
                // No new operation is needed, so copy the diagonal value.
                DP[i][j] = DP[i - 1][j - 1];
            } else {
                // Option 1: delete X[i-1].
                // Then we still need to transform X[0..i-2] into Y[0..j-1].
                int del = DP[i - 1][j] + 1;

                // Option 2: insert Y[j-1] into X.
                // Then we still need to transform X[0..i-1] into Y[0..j-2].
                int ins = DP[i][j - 1] + 1;

                // Option 3: substitute X[i-1] with Y[j-1].
                // Then the remaining prefixes are X[0..i-2] and Y[0..j-2].
                int sub = DP[i - 1][j - 1] + 1;

                // Choose the cheapest of delete, insert, and substitute.
                DP[i][j] = min(min(del, ins), sub);
            }
        }
    }

    // The bottom-right table cell is the answer for the full strings.
    return DP[n][m];
}</code></pre>
<b>Complexity:</b> <code>O(nm)</code> time and <code>O(nm)</code> space."	Info2::Dynamic_Programming
"<b>Exam 22 DP — Minus Process</b> — Given an array <code>A</code> of positive integers, repeatedly remove two values <code>a</code> and <code>b</code>; if <code>a != b</code>, insert <code>|a-b|</code>. Compute the smallest possible final value <code>M</code>. Explain the subset-sum DP and write <code>calculate(A, n)</code>."	"<b>Problem:</b><br>
The Minus process is equivalent to splitting the numbers into two groups. At the end, the remaining value is the absolute difference between the two group sums.
<br><br>
If the total sum is <code>S</code>, the best possible result is obtained by choosing one group with sum as close as possible to <code>S/2</code>. If that group has sum <code>x</code>, the other group has sum <code>S - x</code>, so the final value is:
<pre><code>(S - x) - x = S - 2x</code></pre>
We want the largest reachable <code>x &lt;= S/2</code>.
<br><br>
<b>DP meaning:</b><br>
Let <code>dp[i][j]</code> be the maximum sum not larger than <code>j</code> that can be built using the first <code>i</code> elements <code>A[0..i-1]</code>.
<br><br>
<b>Variable meanings:</b>
<ul>
<li><code>A[]</code>: input array of positive integers.</li>
<li><code>n</code>: number of elements.</li>
<li><code>S</code>: total sum of all elements.</li>
<li><code>half</code>: target upper bound <code>floor(S/2)</code>.</li>
<li><code>dp[i][j]</code>: best reachable sum <code>&lt;= j</code> using the first <code>i</code> values.</li>
<li><code>take</code>: result if we put <code>A[i-1]</code> into the selected group.</li>
<li><code>skip</code>: result if we do not select <code>A[i-1]</code>.</li>
</ul>
For element <code>A[i-1]</code>:
<ul>
<li>If <code>A[i-1] &lt;= j</code>, either take it or skip it.</li>
<li>If <code>A[i-1] &gt; j</code>, it cannot be part of the subsequence, so we skip it.</li>
</ul>
<b>C code:</b>
<pre><code>int max(int a, int b) {
    // Return the larger of two candidate subset sums.
    return a &gt; b ? a : b;
}

int calculate(int A[], int n) {
    // S will store the total sum of all numbers in A.
    int S = 0;

    // Add every array element to the total sum S.
    for (int i = 0; i &lt; n; i++) {
        // Include A[i] in the total.
        S += A[i];
    }

    // We only need subset sums up to floor(S / 2).
    // A subset larger than half would just mirror a smaller subset on the other side.
    int half = S / 2;

    // dp[i][j] = largest reachable sum &lt;= j
    // using only the first i elements A[0..i-1].
    int dp[n + 1][half + 1];

    // First column:
    // If the allowed sum is 0, the best reachable sum is always 0.
    for (int i = 0; i &lt;= n; i++) {
        dp[i][0] = 0;
    }

    // First row:
    // With 0 elements, we cannot build any positive sum.
    // The best reachable sum is therefore 0 for every capacity j.
    for (int j = 0; j &lt;= half; j++) {
        dp[0][j] = 0;
    }

    // Build the table row by row.
    // Row i means: we may use A[0], ..., A[i-1].
    for (int i = 1; i &lt;= n; i++) {
        // Column j means: selected sum may not exceed j.
        for (int j = 1; j &lt;= half; j++) {
            // Check whether the current element A[i-1] fits into capacity j.
            if (j &gt;= A[i - 1]) {
                // Option 1: take A[i-1].
                // Then the remaining allowed sum is j - A[i-1].
                int take = dp[i - 1][j - A[i - 1]] + A[i - 1];

                // Option 2: skip A[i-1].
                // Then the best value is the one from the previous row.
                int skip = dp[i - 1][j];

                // Choose the larger reachable sum that still does not exceed j.
                dp[i][j] = max(take, skip);
            } else {
                // A[i-1] is too large to fit into capacity j.
                // We must skip it and copy the previous row.
                dp[i][j] = dp[i - 1][j];
            }
        }
    }

    // x is the largest reachable subset sum not larger than S/2.
    // This makes the two final group sums as balanced as possible.
    int x = dp[n][half];

    // One group has sum x.
    // The other group has sum S - x.
    // The smallest possible final Minus value is their difference.
    return S - 2 * x;
}</code></pre>
<b>Complexity:</b> <code>O(nS)</code> time and <code>O(nS)</code> space, more precisely <code>O(n · floor(S/2))</code>."	Info2::Dynamic_Programming
"<b>HT</b> — How do you initialize a hash table in C?"	"<pre><code>// H : a POINTER to the hash_table we want to initialize.
//     we pass a pointer (not the struct itself) so the function can
//     modify the original H — without a pointer it would only edit
//     a local copy and the caller would see no changes.
// m : the desired number of slots in the table (e.g. 13).
void init_HT(struct hash_table *H, int m) {

    // H-&gt;m = m  is shorthand for  (*H).m = m
    // it writes the value m into the &quot;m&quot; field of the hash_table
    // struct that H points to.
    H-&gt;m = m;

    // allocate space for m slots on the heap and store the address
    // of the first slot into H-&gt;table.
    // after this, H-&gt;table[i] gives direct access to slot i.
    H-&gt;table = malloc(m * sizeof(struct slot));

    // every slot must start as EMP, otherwise insert/search/delete
    // would read uninitialized garbage in the status field.
    for (int i = 0; i &lt; m; i++) {
        H-&gt;table[i].status = EMP;
    }
}</code></pre>
<b>Quick reminder on the arrow syntax:</b>
<ul>
<li><code>H</code> is a pointer to a struct.</li>
<li><code>H-&gt;m</code> means &quot;the field <code>m</code> of the struct that <code>H</code> points to&quot;.</li>
<li>It is identical to <code>(*H).m</code> — just shorter and more readable.</li>
</ul>"	Info2::Hash_Tables
"<b>HT</b> — How do you define a hash function in C? How do you define the hash functions for <b>double hashing</b>?"	"<b>Single hash function (division method):</b>
<pre><code>// returns the slot index for key k in a table of size m
int h(int k, int m) {
    return k % m;
}</code></pre>
<b>Single hash function (multiplication method):</b>
<pre><code>#include &lt;math.h&gt;

int h(int k, int m) {
    double A = 0.6180339887;          // Knuth's golden-ratio constant
    return (int)(m * fmod(k * A, 1)); // floor( m · ( kA mod 1 ) )
}</code></pre>
<b>Double hashing — TWO hash functions:</b>
<pre><code>// primary hash: gives the starting slot
int h1(int k, int m) {
    return k % m;
}

// secondary hash: gives the STEP SIZE between probes.
// must never return 0 and must be coprime with m.
// typical choice: 1 + (k mod m'), with m' = m - 1 (or m - 2) prime.
int h2(int k, int m) {
    int m_prime = m - 1;              // pick a prime m' &lt; m
    return 1 + (k % m_prime);
}

// the i-th probe position for key k
int probe(int k, int i, int m) {
    return (h1(k, m) + i * h2(k, m)) % m;
}</code></pre>
<b>Reminder:</b> the hash is computed FROM the key (e.g. <code>k = 18, m = 13 → h(k) = 5</code>). The key is what we store; the hash is just the slot index."	Info2::Hash_Tables
"<b>HT</b> — Code: define a hash table with <b>chaining</b> for collision resolution in C. Explain the difference between the two common variants (with vs without <code>*</code> on the table)."	"<b>The node struct (same for both variants):</b>
<pre><code>struct node {
    char name[20];           // the key (could also be int, etc.)
    struct node *next;       // pointer to the next node in the chain
};</code></pre>

<b>Variant A — array of POINTERS (the standard chaining setup):</b>
<pre><code>struct node *HashTable[5000];   // 5000 pointers to nodes</code></pre>
<ul>
<li>Each slot is a <b>pointer</b> to the head of a chain, or <code>NULL</code> if the bucket is empty.</li>
<li>Nodes are allocated on the heap with <code>malloc</code> and linked into a chain via <code>next</code>.</li>
<li>Empty bucket = <code>HashTable[i] == NULL</code> — easy to check.</li>
<li>Memory: 5000 × pointer size (≈ 40 KB on a 64-bit system, regardless of how many keys are stored).</li>
</ul>

<b>Variant B — array of NODES (no <code>*</code>):</b>
<pre><code>struct node HashTable[5000];   // 5000 actual node structs, inline</code></pre>
<ul>
<li>Each slot is an <b>actual node struct</b> stored directly in the array — <code>name</code> and <code>next</code> live inline.</li>
<li>You need a sentinel to mark empty buckets (e.g. <code>name[0] == '\0'</code>), because there is no &quot;NULL slot&quot; — every slot is already a full struct.</li>
<li>Additional collided keys still need <code>malloc</code> and are linked via the embedded <code>next</code> pointer.</li>
<li>Memory: 5000 × sizeof(struct node) (much larger up front, e.g. ~140 KB), even if most buckets are empty.</li>
</ul>

<b>Typical insertion (variant A — &quot;insert at head&quot;):</b>
<pre><code>void insert_chain(struct node *HashTable[], int idx, struct node *new_node) {
    new_node-&gt;next  = HashTable[idx];   // new node points to old head
    HashTable[idx]  = new_node;          // new node becomes the new head
}</code></pre>

<b>Which one to use?</b> Almost always <b>Variant A</b> (array of pointers): cheaper memory when buckets are mostly empty, cleaner empty-bucket check, no risk of confusing &quot;inline slot&quot; with &quot;chain node&quot;."	Info2::Hash_Tables
"<b>HT — Chaining</b> — What is the load factor for chaining? What is the search complexity? What is the expected number of probes? <b>Bonus:</b> what benefits and disadvantages would you expect by using a <i>binary search tree</i> instead of a linked list to handle hash-table overflows?"	"<b>Chaining:</b> each slot holds a linked list (&quot;chain&quot;) of all keys that hash to that slot.
<br><br>
<b>Load factor:</b>
<pre><code>α = n / m</code></pre>
where <code>n</code> = number of stored keys and <code>m</code> = number of buckets. For chaining <b>α can exceed 1</b> (chains can be arbitrarily long).
<br><br>
<b>Search complexity:</b>
<ul>
<li><b>Average:</b> <code>O(1 + α)</code> — constant hashing time plus a walk through one chain of expected length α.</li>
<li><b>Worst case:</b> <code>O(n)</code> — if all keys hash to the same bucket, one chain holds everything.</li>
</ul>
<b>Expected number of probes:</b>
<ul>
<li><b>Unsuccessful search:</b> <code>≈ α</code> (must walk the full chain).</li>
<li><b>Successful search:</b> <code>≈ 1 + α / 2</code> (on average we find the key in the middle of the chain).</li>
</ul>
<br>
<b>Bonus — BST instead of linked list for the chains:</b>
<br><br>
<b>Benefits:</b>
<ul>
<li><b>Faster lookup inside a heavy bucket:</b> a linked list takes <code>O(chain length)</code>, a balanced BST (e.g. AVL) takes <code>O(log(chain length))</code>.</li>
<li><b>Much better worst case:</b> if every key collides into one bucket, the worst case drops from <code>O(n)</code> to <code>O(log n)</code>.</li>
<li><b>Ordered traversal inside a bucket</b> becomes possible (useful for range queries).</li>
</ul>
<b>Disadvantages:</b>
<ul>
<li><b>More memory per node:</b> instead of one <code>next</code> pointer, a BST node needs <code>left</code>, <code>right</code> (and a height field for AVL).</li>
<li><b>More code complexity:</b> insertion, deletion and (for AVL) rebalancing must be implemented per bucket.</li>
<li><b>Pointless if α is small:</b> when chains are short (e.g. average length 1–2), the constant overhead of trees costs more than it saves — a plain list wins in practice.</li>
</ul>
<b>Rule of thumb:</b> use trees as the bucket structure only when collisions are very heavy (e.g. Java's <code>HashMap</code> switches a bucket from a linked list to a red-black tree once the bucket has &gt; 8 entries)."	Info2::Hash_Tables
"<b>HT — Open addressing</b> — How does it work (principle)? What is the load factor? How do <b>insert</b>, <b>search</b>, and <b>delete</b> work?"	"<b>Principle:</b>
<ul>
<li><b>All elements are stored directly in the table array</b> — no linked lists, no chains.</li>
<li>On a collision, we <b>probe</b> for the next slot using a probe sequence: <code>h(k, 0), h(k, 1), h(k, 2), …</code></li>
<li><b>Insert:</b> probe until we find an EMP or DEL slot.</li>
<li><b>Search:</b> probe until we find the key (OCC + same key) or hit an EMP slot (then key is not in the table).</li>
<li><b>Delete:</b> mark the slot as DEL (tombstone), <i>not</i> EMP — otherwise we would break the probe path of other keys.</li>
</ul>
<b>Load factor:</b>
<pre><code>α = n / m,    with    α ≤ 1</code></pre>
<b>α cannot exceed 1</b> because every slot holds at most one element. In practice keep <code>α ≤ 0.7</code> for good performance; if it grows beyond that, resize the table."	Info2::Hash_Tables
"<b>HT</b> — What is the formula for <b>linear probing</b>? <b>Exercise:</b> insert keys <b>18</b> and <b>44</b> into a HT of size <b>13</b> using <b>linear probing with step size 1</b> and the <b>division method</b>."	"<b>Formula:</b>
<pre><code>h(k, i) = ( h'(k) + i )  mod  m</code></pre>
where:
<ul>
<li><code>h'(k)</code> is any base hash function (e.g. <code>k mod m</code>).</li>
<li><code>i = 0, 1, 2, …, m − 1</code> is the probe number.</li>
</ul>
So we simply check slot <code>h'(k)</code>, then <code>h'(k) + 1</code>, then <code>h'(k) + 2</code>, … (wrapping mod m).
<br><br>
<b>Drawback:</b> <b>primary clustering</b> — long runs of occupied slots accumulate and slow down every probe that lands inside them.
<br><br>
<b>Exercise — solution (m = 13, h(k) = k mod 13):</b>
<br><br>
<b>Step 1: insert k = 18</b>
<pre><code>h(18, 0) = (18 mod 13 + 0) mod 13 = (5 + 0) mod 13 = 5
slot 5 is EMP → insert 18 at slot 5.</code></pre>

<b>Step 2: insert k = 44 — first probe</b>
<pre><code>h(44, 0) = (44 mod 13 + 0) mod 13 = (5 + 0) mod 13 = 5
slot 5 is now OCC (holds 18) → collision → probe further.</code></pre>

<b>Step 3: insert k = 44 — linear probe with i = 1</b>
<pre><code>h(44, 1) = (5 + 1) mod 13 = 6
slot 6 is EMP → insert 44 at slot 6.</code></pre>
<br>
<b>Final table:</b>
<table style=&quot;border-collapse: collapse;&quot;>
<tr><th>slot</th><th>0</th><th>1</th><th>2</th><th>3</th><th>4</th><th>5</th><th>6</th><th>7</th><th>8</th><th>9</th><th>10</th><th>11</th><th>12</th></tr>
<tr><td>key</td><td>–</td><td>–</td><td>–</td><td>–</td><td>–</td><td><b>18</b></td><td><b>44</b></td><td>–</td><td>–</td><td>–</td><td>–</td><td>–</td><td>–</td></tr>
</table>"	Info2::Hash_Tables
"<b>HT</b> — What is the formula for <b>quadratic probing</b>?"	"<pre><code>h(k, i) = ( h'(k) + c₁ · i + c₂ · i² )  mod  m</code></pre>
where <code>c₁</code> and <code>c₂ ≠ 0</code> are constants and <code>i = 0, 1, 2, …, m − 1</code>.
<br><br>
The probe jumps grow quadratically, which <b>breaks up primary clustering</b>.
<br><br>
<b>Drawback:</b> <b>secondary clustering</b> — any two keys with the same <code>h'(k)</code> follow the exact same probe sequence. Also <code>c₁, c₂, m</code> must be chosen carefully so the probe sequence visits every slot."	Info2::Hash_Tables
"<b>HT</b> — What is the formula for <b>double hashing</b>? What is a good choice for <code>m'</code>? <b>Exercise:</b> insert keys <b>18</b> and <b>44</b> with <code>h₁(k) = k mod 13</code> and <code>h₂(k) = 8 − (k mod 8)</code> (table size m = 13)."	"<b>Formula:</b>
<pre><code>h(k, i) = ( h₁(k) + i · h₂(k) )  mod  m</code></pre>
where <code>h₁</code> is the primary hash function and <code>h₂</code> is a <b>second</b> hash function used as the step size.
<br><br>
<b>Requirements on h₂:</b>
<ul>
<li><code>h₂(k) ≠ 0</code> for all keys.</li>
<li><code>h₂(k)</code> must be <b>relatively prime to m</b>, otherwise the probe sequence will not visit every slot.</li>
</ul>
<b>Typical choice:</b>
<pre><code>h₂(k) = 1 + ( k mod m' )</code></pre>
where <code>m'</code> is slightly smaller than <code>m</code>, e.g. <code>m' = m − 1</code> or <code>m' = m − 2</code>, chosen <b>prime</b>. This guarantees the &quot;coprime with m&quot; requirement.
<br><br>
<b>Why double hashing is the best of the three:</b> different keys with the same <code>h₁(k)</code> get <i>different</i> probe sequences (because their <code>h₂(k)</code> differs) → almost no clustering.
<br><br>
<b>Exercise — solution:</b>
<br><br>
<b>Step 1: insert k = 18</b>
<pre><code>h₁(18) = 18 mod 13 = 5
slot 5 is EMP → insert 18 at slot 5.   (h₂ not needed yet)</code></pre>

<b>Step 2: insert k = 44 — first probe (i = 0)</b>
<pre><code>h₁(44) = 44 mod 13 = 5
h(44, 0) = (5 + 0 · h₂(44)) mod 13 = 5
slot 5 is OCC (holds 18) → collision → probe with i = 1.</code></pre>

<b>Step 3: compute the step size <code>h₂(44)</code></b>
<pre><code>h₂(44) = 8 − (44 mod 8) = 8 − 4 = 4</code></pre>

<b>Step 4: insert k = 44 — second probe (i = 1)</b>
<pre><code>h(44, 1) = (h₁(44) + 1 · h₂(44)) mod 13
         = (5 + 4) mod 13
         = 9
slot 9 is EMP → insert 44 at slot 9.</code></pre>

<b>Final table:</b>
<table style=&quot;border-collapse: collapse;&quot;>
<tr><th>slot</th><th>0</th><th>1</th><th>2</th><th>3</th><th>4</th><th>5</th><th>6</th><th>7</th><th>8</th><th>9</th><th>10</th><th>11</th><th>12</th></tr>
<tr><td>key</td><td>–</td><td>–</td><td>–</td><td>–</td><td>–</td><td><b>18</b></td><td>–</td><td>–</td><td>–</td><td><b>44</b></td><td>–</td><td>–</td><td>–</td></tr>
</table>"	Info2::Hash_Tables
"<b>HT</b> — Deletion in a hash table: why can we NOT just set a slot to EMP? Code deletion in C. Then extend the algorithm so that deletion not only removes <code>k</code> but also <b>rearranges the other elements as if <code>k</code> had never been inserted</b>."	"<b>Why not just set it to EMP?</b>
<br><br>
Because that would break <i>search</i> for other keys that probed past this slot during insertion. Example with linear probing:
<ol>
<li>Insert <code>k₁</code> → lands at slot 3.</li>
<li>Insert <code>k₂</code> with <code>h(k₂) = 3</code> → slot 3 is taken → linear probe → lands at slot 4.</li>
<li>Now delete <code>k₁</code> and set slot 3 to <code>EMP</code>.</li>
<li>Search for <code>k₂</code>: hash to slot 3, see <code>EMP</code>, return &quot;not found&quot;.<br>
But <code>k₂</code> is sitting at slot 4! Bug.</li>
</ol>
<b>Two solutions to this problem:</b>
<ol>
<li><b>Tombstone (DEL):</b> mark the slot as DEL. Search treats DEL like OCC (keeps probing); insert treats DEL like EMP (slot can be reused).</li>
<li><b>Rehash on delete:</b> mark the slot EMP <i>and</i> re-insert every following element until we hit an EMP slot — so the table looks as if <code>k</code> was never there.</li>
</ol>
<br>
<b>Variant 1 — simple deletion with DEL tombstone:</b>
<pre><code>// marks the slot of key k as DELETED (tombstone).
// returns the slot index on success, -1 if k is not in the table.
int hash_delete(struct hash_table *H, int k) {
    for (int i = 0; i &lt; H-&gt;m; i++) {
        int idx = (h(k) + i) % H-&gt;m;

        // EMP slot → k cannot be further along the probe path
        if (H-&gt;table[idx].status == EMP) return -1;

        // OCC and key matches → tombstone it
        if (H-&gt;table[idx].status == OCC &amp;&amp; H-&gt;table[idx].key == k) {
            H-&gt;table[idx].status = DEL;
            return idx;
        }

        // DEL slot → skip, keep probing
    }
    return -1;   // searched the whole table, key not found
}</code></pre>
<br>
<b>Variant 2 — deletion with rehashing (no DEL state needed).</b><br>
<b>Step-by-step logic:</b>
<ol>
<li>Find the slot <code>j</code> that holds <code>k</code> (standard search). Mark it <code>EMP</code>.</li>
<li>Walk forward through the probe sequence: <code>j+1, j+2, …</code> (mod m), until you hit an <code>EMP</code> slot.</li>
<li>Every <code>OCC</code> slot you visit on the way might have ended up where it is only because <code>k</code> was blocking an earlier slot. So: <b>lift it out</b> (save the key, mark the slot EMP) and <b>re-insert it</b>.</li>
<li>To keep the loop terminating, do this in two passes: first <i>collect &amp; empty</i> every OCC slot up to the next EMP, then re-insert all collected keys.</li>
<li>Result: the table is exactly what it would have been if <code>k</code> had never been inserted — and we never need a DEL state.</li>
</ol>
<pre><code>// deletes k and then rehashes every element that came after k in
// k's probe path, so the table looks as if k had never been inserted.
int hash_delete_rehash(struct hash_table *H, int k) {
    // 1) find the slot of k (standard search)
    int j = -1;
    for (int i = 0; i &lt; H-&gt;m; i++) {
        int idx = (h(k) + i) % H-&gt;m;
        if (H-&gt;table[idx].status == EMP) return -1;
        if (H-&gt;table[idx].status == OCC &amp;&amp; H-&gt;table[idx].key == k) {
            j = idx;
            break;
        }
    }
    if (j == -1) return -1;

    // 2) plain delete: mark slot j EMP (no tombstone)
    H-&gt;table[j].status = EMP;

<span style=&quot;color:#ff6b6b;&quot;>    // 3) REARRANGE — collect &amp; empty every OCC slot following j
    //    (up to the next EMP), then re-insert each saved key.
    int saved_keys  [H-&gt;m];
    int saved_values[H-&gt;m];
    int count = 0;
    int i = (j + 1) % H-&gt;m;
    while (H-&gt;table[i].status == OCC) {
        saved_keys  [count] = H-&gt;table[i].key;
        saved_values[count] = H-&gt;table[i].value;
        H-&gt;table[i].status = EMP;
        count++;
        i = (i + 1) % H-&gt;m;
    }
    // re-insert all collected elements
    for (int idx = 0; idx &lt; count; idx++) {
        hash_insert(H, saved_keys[idx], saved_values[idx]);
    }</span>

    return j;
}</code></pre>
<b>Cost:</b> deletion is now O(<i>length of the OCC run after j</i>) instead of O(1), but searches stay simple and there are no tombstones cluttering the table."	Info2::Hash_Tables
"<b>HT</b> — Code: insert a key in a hash table with <b>linear probing</b>."	"<pre><code>// inserts (k, v) into the hash table H.
// returns the slot index on success, -1 if the table is full.
int hash_insert(struct hash_table *H, int k, int v) {
    // i = probe number;   idx = slot to check at this probe
    for (int i = 0; i &lt; H-&gt;m; i++) {
        int idx = (h(k) + i) % H-&gt;m;

        // we can reuse EMP slots AND DEL tombstones
        if (H-&gt;table[idx].status != OCC) {
            H-&gt;table[idx].key    = k;
            H-&gt;table[idx].value  = v;
            H-&gt;table[idx].status = OCC;
            return idx;
        }

        // OCC slot → collision, advance the probe
    }
    return -1;   // table is full
}</code></pre>
<b>Note:</b> this assumes <code>k</code> is not already in the table. If duplicates must be rejected, first run <code>hash_search(H, k)</code> and only insert if the search returns -1."	Info2::Hash_Tables
"<b>HT</b> — Code: search for a key <code>k</code> in a hash table with <b>linear probing</b>."	"<pre><code>// searches for key k in the hash table H.
// returns the slot index if found, -1 otherwise.
int hash_search(struct hash_table *H, int k) {
    for (int i = 0; i &lt; H-&gt;m; i++) {
        int idx = (h(k) + i) % H-&gt;m;

        // EMP means: slot was never used. linear probing would have
        // placed k HERE if it had ever been inserted → it isn't in
        // the table → stop searching.
        if (H-&gt;table[idx].status == EMP) return -1;

        // OCC and key matches → found
        if (H-&gt;table[idx].status == OCC &amp;&amp; H-&gt;table[idx].key == k) {
            return idx;
        }

        // DEL → skip and continue probing (don't stop here!)
    }
    return -1;   // searched the whole table, key not found
}</code></pre>
<b>Key detail:</b> stop on EMP but <i>never</i> on DEL — that is exactly why we need the DEL tombstone."	Info2::Hash_Tables
"<b>HT Exercise — Duplicates / Intersection</b> — You are given two arrays <code>A</code> and <code>B</code> with lengths <code>n</code> and <code>m</code>, containing strictly positive integers. Describe how hash tables can be used to print all elements that appear in both arrays (<code>A ∩ B</code>). Give a short explanation and pseudocode. The algorithm should run in <code>O(n + m)</code>."	"<b>Idea:</b>
<ul>
<li>Store all elements of <code>A</code> in a hash table.</li>
<li>Then scan <code>B</code>. For each <code>B[j]</code>, search it in the hash table.</li>
<li>If it is found, print it because it appears in both arrays.</li>
</ul>
<b>Important detail:</b> if <code>B</code> may contain duplicates and you only want to print each common element once, delete the element from the hash table after printing it. Then later occurrences of the same value in <code>B</code> will not be printed again.
<br><br>
<b>Pseudocode:</b>
<pre><code>void print_intersection(int A[], int n, int B[], int m) {
    HT H;
    init_HT(&amp;H);

    // put every element of A into the hash table
    for (int i = 0; i &lt; n; i++) {
        insert(&amp;H, A[i]);
    }

    // check which elements of B also occur in A
    for (int j = 0; j &lt; m; j++) {
        if (search(&amp;H, B[j]) != -1) {
            printf(&quot;%d &quot;, B[j]);

            // optional but useful: prevents printing duplicates
            // if B contains the same common element several times
            delete(&amp;H, B[j]);
        }
    }
}</code></pre>
<b>Complexity argument:</b>
<ul>
<li>Building the hash table from <code>A</code>: expected <code>O(n)</code>.</li>
<li>Scanning <code>B</code> and searching each element: expected <code>O(m)</code>.</li>
<li>Total expected time: <b><code>O(n + m)</code></b>.</li>
<li>Extra space: <code>O(n)</code> for the hash table.</li>
</ul>"	Info2::Hash_Tables
"<b>HT Exercise — Target Sum</b> — You are given an array <code>A</code> with <code>n</code> strictly positive, distinct integers and a target sum <code>t</code>. Use a hash table to decide whether any two integers in <code>A</code> form the target sum <code>t</code>. Return <code>1</code> if possible and <code>0</code> otherwise. The algorithm should run in <code>O(n)</code>."	"<b>Idea:</b>
<ul>
<li>For each element <code>x = A[i]</code>, the partner we need is <code>t − x</code>.</li>
<li>Use a hash table to remember elements we have already seen.</li>
<li>Before inserting <code>x</code>, search for <code>t − x</code>.</li>
<li>If the complement is already in the hash table, we found a valid pair.</li>
</ul>
<b>Why search before insert?</b> Because we must use <b>two different elements</b>. If we inserted <code>x</code> first, then for <code>t = 2x</code> we might accidentally match <code>x</code> with itself. The array is distinct, but this order is still the clean invariant.
<br><br>
<b>Pseudocode:</b>
<pre><code>int target_sum(int A[], int n, int t) {
    HT H;
    init_HT(&amp;H);

    for (int i = 0; i &lt; n; i++) {
        int x = A[i];
        int needed = t - x;

        // have we already seen the required partner?
        if (search(&amp;H, needed) != -1) {
            return 1;
        }

        // remember x for later elements
        insert(&amp;H, x);
    }

    return 0;
}</code></pre>
<b>Small example:</b> if <code>A = [3, 9, 2, 8]</code> and <code>t = 11</code>, then when <code>x = 2</code>, <code>needed = 9</code>. Since 9 was inserted earlier, return <code>1</code>.
<br><br>
<b>Complexity argument:</b>
<ul>
<li>Each element is searched once and inserted once.</li>
<li>Hash table operations are expected <code>O(1)</code>.</li>
<li>Total expected time: <b><code>O(n)</code></b>.</li>
<li>Extra space: <code>O(n)</code>.</li>
</ul>"	Info2::Hash_Tables
